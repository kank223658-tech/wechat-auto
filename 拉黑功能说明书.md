# 拉黑功能说明书

一比一复刻参考视频《拉黑界面，实现拉黑的功能.mp4》的「联系人设置页」与拉黑全过程动画：

> 对方资料页右上角「…」 → **设置页**（编辑备注 / 设置权限 / 把他(她)推荐给朋友 / 设为星标朋友 / **加入黑名单** / 投诉 / 删除联系人）
> → 拨「加入黑名单」开关（弹回式变绿） → 底部弹起确认弹窗（文案 + 确定/取消）
> → 点「确定」弹窗收起 → 屏幕中央「正在加载」加载动画 → 拉黑完成（开关保持绿色）

---

## 一、脚本用法

### 文本剧本（script.txt / 任一 .txt）

```
[打开聊天] 陆香儿
[打开对方主页] 陆香儿
[打开对方设置]
[加入黑名单] 确定 | 1.4
[等待] 1
[移出黑名单] 确定
```

- `打开对方设置` 后可跟人设名（可空，留空用场景里配置的对方）。
- `加入黑名单` 格式：`[加入黑名单] 确定 | 加载秒`
  - 第 1 段 `确认`：弹窗点哪个按钮，写 `确定` 或 `取消`（写 `取消` 演示取消路径：弹窗收起后开关弹回灰色，无加载动画）。
  - 第 2 段 `加载秒`（可选）：「正在加载」Toast 显示秒数，缺省 1.4。
- `移出黑名单`：把已拉黑的联系人移出来，动画同上（弹窗文案换成移出版本）。

### JSON 工作流（workflow.json / 工作流编辑器）

```json
{"action": "打开对方设置", "params": {"对方": "陆香儿"}}
{"action": "加入黑名单", "params": {"确认": "确定", "加载秒": 1.4, "停留": 0.8}}
{"action": "移出黑名单", "params": {"确认": "确定"}}
```

params 字段：

| 字段 | 说明 | 缺省 |
| --- | --- | --- |
| `确认` | 弹窗按钮：`确定` / `取消` | 确定 |
| `加载秒` | 「正在加载」显示秒数 | 1.4 |
| `停留` | 动画结束后的停留秒数 | 0.8 |
| `对方` | 人设名（仅「打开对方设置」用，可空） | 空 |

### 已登记的别名

`拉黑` `拉黑对方` `拉黑他` `把他拉黑` → `加入黑名单`
`解除拉黑` `取消拉黑` → `移出黑名单`
`打开设置` `打开联系人设置` `联系人设置` `对方设置` `设置页` → `打开对方设置`

---

## 二、动画全过程（与参考视频逐段对齐）

| 阶段 | 画面 | 时长 |
| --- | --- | --- |
| 1. 进入设置页 | 从右侧滑入深色「设置」页，底层页面左移压暗（iOS 推入转场） | 0.34s |
| 2. 拨开关 | 「加入黑名单」开关从灰色弹到绿色（滑块右移 + 底色渐变） | 0.5s |
| 3. 确认弹窗 | 底部弹起半透明深灰面板：文案「加入黑名单，你将不再收到对方的消息，并且你们互相看不到对方朋友圈的更新」+ 红色「确定」+ 白色「取消」，背后压暗 | 0.85s |
| 4. 点确定 | 弹窗下滑收起，压暗消失；开关保持绿色 | 0.4s |
| 5. 正在加载 | 屏幕中央弹出深灰圆角 Toast：白色旋转圆环 + 「正在加载」 | 加载秒（默认 1.4s） |
| 6. 完成 | Toast 淡出，停在设置页，开关绿色 | 停留（默认 0.8s） |

取消路径（确认=取消）：第 4 步弹窗收起后，开关弹回灰色，无加载动画。

---

## 三、入口与交互细节

- **手动点击路径**：聊天页点对方头像 → 对方资料页 → 右上角「…」→ 设置页。三个入口（脚本动作、点「…」、`window.__wxBlock.openSettings()`）效果完全一致。
- **返回**：设置页左上角箭头或 `[返回上一页]` 退回资料页，再退回聊天页（iOS 推出转场）。
- **状态保持**：拉黑后开关保持绿色；`[移出黑名单]` 复位。重复拉黑会报错提醒（避免动画错乱）。
- **前端 API**（可手动调）：`window.__wxBlock.openSettings() / tapToggle() / showSheet() / confirmSheet() / cancelSheet() / showToast(秒) / block(选项) / unblock(选项) / isOpen() / isBlocked() / reset()`。
- 代码位置：界面与动画在 `enhance/block_ui.js` + `enhance/block_ui.css`（由 main.py 自动注入，无需手动加载）；动作执行在 `main.py` 的 `open_peer_settings / block_peer`。

---

## 四、常见问题

- **「加入黑名单」报「已处于拉黑状态」**：上一段流程已经拉黑过了。先 `[移出黑名单]`，或重新 `[打开对方设置]` 前用 `window.__wxBlock.reset()` 复位。
- **设置页下面没有资料页**：脚本直接 `[打开对方设置]` 时会自动补开资料页垫底，返回栈不会乱。
- **取消路径怎么拍**：`{"action": "加入黑名单", "params": {"确认": "取消"}}`，弹窗点「取消」，开关弹回灰色。

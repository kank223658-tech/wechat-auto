# 链接卡片功能说明书

在聊天里发送 / 接收「微信公众号文章、分享链接」样式的卡片气泡，一比一复刻《微信链接样式.jpg》：
深灰卡片底 + 标题左上（自动换行）+ 方形缩略图右侧 + 来源名左下（默认「恋爱技巧」，可替换）。

---

## 一、脚本用法

### 文本剧本（script.txt / 任一 .txt）

```
[我方发链接] 男生脱单其实超简单
[我方发链接] 男生脱单其实超简单 | 男生.jpg | 恋爱技巧
[对方发链接] 对方分享的文章 | /images/link/男生.jpg | 情感电台
```

- `标题`：链接卡片上的文字，会自动换行，字多了卡片会撑高。
- `图片`（可选）：卡片缩略图。可省略（按标题自动匹配）、写关键词/文件名、或写 `/images/...` 绝对路径。
- `来源`（可选）：左下角来源名，默认「恋爱技巧」。
- 末尾追加 ` | 18:22` 可在卡片前加一条时间分隔条。

格式：`[我方发链接] 标题 | 图片 | 来源`（三段，按需省略后两段）。

### JSON 工作流（workflow.json）

```json
{"action": "我方发链接", "params": {"标题": "男生脱单其实超简单", "图片": "男生.jpg", "来源": "恋爱技巧"}}
{"action": "对方发链接", "params": {"标题": "对方分享的文章", "图片": "/images/link/男生.jpg", "来源": "情感电台"}}
```

params 字段：`标题`（必填）、`图片`（可选）、`来源`（可选，默认恋爱技巧）、`时间`（可选）。

### 已登记的别名

`发链接` `发送链接` `分享链接` `发文章` `发送文章` `发卡片` `发送卡片` `我发链接` → `我方发链接`
`对面发链接` `对方发送链接` `对方发文章` → `对方发链接`

---

## 二、卡片缩略图（图片）怎么配

### 1. 上传缩略图
把方形缩略图放进目录：`vue-WeChat/public/images/link/`

文件名上带一个能代表该链接主题的关键词即可，例如：`男生.jpg`、`脱单.jpg`、`副业.jpg`、`恋爱.jpg`。

### 2. 图片按「标题里的字」自动匹配（推荐）
标题里出现关键词（文件名去扩展名后与标题互相包含）即自动命中：

```
[我方发链接] 男生脱单其实超简单     → 命中 link 目录里的 男生.jpg
```

### 3. 精确匹配（可选）：link_library.json
在项目根目录 `link_library.json` 登记：

```json
[
  {"url": "/images/link/男生.jpg", "name": "脱单", "keywords": "男生,脱单,恋爱,简单"}
]
```

- `url`：缩略图路径。
- `name` / `keywords`：用于按标题或脚本显式指定时做精准匹配的词语。
- 脚本里写 `[我方发链接] 标题 | 脱单`（关键词）也会命中该 url。

> 匹配优先级：`/images/...` 绝对路径 > 图库关键词/名字 > 文件名包含 > 标题自动匹配（图片留空时）。

---

## 三、来源名「恋爱技巧」怎么改

把第三段写成想要的字即可，脚本每次可换：

```
[我方发链接] 男生脱单其实超简单 | 男生.jpg | 情感电台
[对方发链接] 某某文章 | 男生.jpg | 我的公众号
```

---

## 四、示例文件

- `link_demo.txt`：文本剧本示例（含自动匹配 / 显式指定 / 替换来源 / 对方发送 / 时间分隔条）。
- `link_workflow.json`：JSON 工作流示例。

运行：

```
python main.py --script link_demo.txt
python main.py --workflow link_workflow.json
```

# 微信聊天录屏 → 剧本 转换攻略（给 Gemini 用）

> 你（Gemini）的任务：看一段「微信聊天界面录屏视频」，把它忠实转换成一套本播放器能直接执行的剧本文本。剧本是一行一条的指令文本，形如 `[动作名] 参数`。
> 铁律只有三条：**逐字照抄视频里出现的文字，绝不改写**；**只用本攻略动作表里的动作名**；**视频里没有的情节绝不添加**。

---

## 一、剧本总结构

一份完整剧本按顺序由三部分组成：

```
（第 1 部分，可选）[历史会话] …… [历史会话结束]   ← 视频开头展示的聊天列表
（第 2 部分）[打开聊天] 某人                        ← 进入会话，实时对白开始
（第 3 部分）实时指令若干行，按视频时间顺序排列
```

- 空行会被忽略；`#` 开头的行是注释，会被忽略。
- 每条指令独占一行，必须以 `[动作名]` 开头。**动作名不在动作表里的行会被静默丢弃**（不报错、直接消失），所以动作名必须一字不差照抄本攻略。
- 指令顺序 = 视频里事件发生的先后顺序。

---

## 二、历史会话块（视频开头的聊天列表）

如果视频开头展示了微信主页的聊天列表（哪怕一闪而过），把它转成：

```
[历史会话]
[会话] 陆香儿
陆香儿：图片怎么发您 | 18:40
我：我教你，先点加号 | 18:41
陆香儿：[图片] 风景照
陆香儿：[链接] 摄影入门十讲 | 风景照 | 摄影笔记 | 16:20
[会话] 王姐
王姐：明天有空吗 | 昨天 21:05
……
[历史会话结束]
```

规则：
- `[会话] 名字` 切分每个会话；我方发过的消息写 `我：`，其余人写 `名字：`。
- 时间照抄视频里列表显示的值，写在 `|` 后面。支持格式：`18:40`、`昨天 21:05`、`星期三`、`5分钟前`。没显示时间就不写。
- 图片消息写成 `[图片] 内容描述短语`；表情贴纸写成 `[表情] 描述短语`；语音写成 `[语音] 秒数`；链接/小程序卡片写成 `[链接] 标题 | 封面描述 | 来源名 | 时间`。
- 对白文字里带小黄脸的照抄成内嵌 `[名称]`（进会话后自动渲染成行内表情）。
- **主页一屏最多 9 个会话**，只转前 9 个，多余的丢弃。
- 头像不用写，系统会按人名自动套人物库。
- `[历史会话]` 块只放历史消息；`[打开聊天]` 和实时对白必须写在块外面。

---

## 三、行内语法细则

### 3.1 两种参数写法
- **单参数动作**用位置写法：`[打开聊天] 陆香儿`、`[等待] 2`。
- **多参数动作**必须用命名写法：`[切换底部面板] 面板=表情 | 停留=0.8`、`[转账] 接收人=陆香儿 | 金额=520.00 | 备注=别熬夜`。命名写法不会丢参数。

### 3.2 `|` 分段的特殊约定（只有这几个动作）
| 动作 | 写法 | 说明 |
|---|---|---|
| 打字不发 | `[打字不发] 键盘字幕 \| 停留秒数 \| 插话1；插话2` | 第 1 段=**打给观众看的教学字幕/博弈内容**（在键盘上打出来、不发送，这是整条视频最精彩的卖点，照视频原样抄）；第 2 段**只有纯数字才算停留**（如 `0.5`、`1.5`），写了文字就整体算插话；第 3 段=对方边看你打字边发的插话，多句用「；」隔开。**插话支持全部消息格式**（按行首标记路由）：`[对方表情] 素材短名`=贴纸、`[对方图片] 素材短名`=图片、`[对方链接] 标题 \| 封面 \| 来源`=链接卡片、`[对方emoji] 微笑`=3D黄脸（名称/编号/随机）、`[对方语音] 5`=语音条（秒数）、`[对方转账] 520.00 \| 备注`=转账卡片；**无标记=纯文字**，文字里的 `[微笑]` 会按内嵌 emoji 渲染 |
| 我方打字 | `[我方打字] 内容 \| 对方插话` | 打字期间对方插话；多句用「；」隔开。**例外**：内容以 `[链接]` 开头时 `|` 属于链接参数，不当插话 |
| 我方发链接 / 对方发链接 | `[我方发链接] 标题 \| 封面描述 \| 来源名 \| 18:22` | 来源缺省为「恋爱技巧」；时间只能写在最后 |
| 对方后台发消息 | `[对方后台发消息] 联系人 \| 内容` | 内容以 `[图片]` 开头 = 图片消息 |

### 3.3 时间分隔条
给某条消息挂「会话内灰色时间条」：把时间写在**该行的最后一段**，如 `[对方发消息] 好久不见 | 19:21`。支持 `HH:MM` / `昨天 HH:MM`。只有带消息的动作能挂（我方打字、对方发消息、发送图片、对方发图片、发送表情、对方表情、发送emoji、对方emoji、我方发链接、对方发链接、对方后台发消息等）。
**整份剧本实时部分最多 3 处时间条**，只保留换天、隔很久这类关键节点；视频里出现太密就挑最关键的 3 处，其余丢掉。

---

## 四、动作总表（只能用这些动作名）

### 聊天类（核心）

| 指令写法 | 用途 |
|---|---|
| `[打开聊天] 联系人` | 从聊天列表进入会话；实时对白的第一步 |
| `[我方打字] 内容` | 我方打字并发送；可加 `\| 对方插话`、行尾 `\| 18:22` |
| `[打字不发] 键盘字幕 \| 0.5` | 在键盘上打出「给观众看的教学字幕/博弈内容」（如 `故意否定 引起注意`），停在输入框不发送、停留 0.5 秒；可再接 `\| 对方插话`（支持 `[对方表情] 素材短名` 表情包插话） |
| `[删除文字] -1` | 清空输入框（正数=删几个字）。**紧跟 [打字不发] 之后** |
| `[对方正在输入] 1.2` | 顶部显示「对方正在输入…」1.2 秒；不要每条回复前都用 |
| `[对方发消息] 内容` | 对方白色气泡；允许照视频连发 2~3 条，一条一行 |
| `[发送图片] 图片=短名 \| 打开=是 \| 停留=0.6` | 我方发图；「打开=是」发完自动点开查看 |
| `[对方发图片] 图片=短名 \| 打开=是 \| 停留=0.6` | 对方发图 |
| `[发送表情] 表情名` | 我方发表情贴纸（78×78 小气泡） |
| `[对方表情] 表情名` | 对方发表情贴纸 |
| `[对方后台发表情] 联系人 \| 表情名` | 未打开的会话收到表情 |
| `[发送emoji] 害羞` | 我方发 3D 黄脸 emoji，弹笑脸面板逐个点选后发送；多个用逗号：`3,5,8`；**可用名称共 110 个**（微笑/呲牙/大笑/爱心/捂脸/握手/墨镜/害羞/皱眉/花痴/流汗/生气/羞涩/闭嘴/惊恐/装酷/大哭/调皮/憨笑/哭泣/委屈/愤怒/难过/尴尬/抓狂/惊讶/无语/困/恐惧，以及 翻白眼/开心/疑问/晕/托腮/挖鼻/拜托/嘟嘴/爱心眼/坏笑/吃瓜/狗头/星星眼/玫瑰/蛋糕/红包封/烟花/猪头/企鹅 等），或 **1~110 编号**，或 `随机`。看不清就用编号或 `随机` 并记入存疑列表 |
| `[对方emoji] 捂脸` | 对方发 3D 黄脸 emoji，直接上屏 |
| `[我方打字] 太开心了[大笑]` | **内嵌写法（优先）**：把 `[名称]` 直接写进文本里，随文字一起上屏。适用于 `[我方打字]`、`[打字不发]`、`[对方发消息]`、`[对方后台发消息]` 的内容以及插话；名称必须是上面名称表里的名字或别名，认不出的 `[词]` 会原样显示成文字 |
| `[我方发链接] 标题 \| 封面描述 \| 来源名` | 我方发链接卡片 |
| `[对方发链接] 标题 \| 封面描述 \| 来源名` | 对方发链接卡片 |
| `[对方后台发消息] 联系人 \| 内容` | 别的会话来消息（画面不切走，只刷主页预览+未读角标） |
| `[切换底部面板] 面板=表情 \| 停留=0.8` | 直接切底部面板（键盘/表情/更多/收起） |
| `[转发消息] 转发：xxx` | 转发一条消息 |
| `[手机状态栏] 转账` | 状态栏切参考场景（03:14+录屏红点）；`默认` 恢复 18:36 |
| `[转账] 接收人=某人 \| 金额=520.00 \| 备注=xxx` | 我方转账卡片 |
| `[对方转账] 接收人=我 \| 金额=200.00 \| 备注=xxx` | 对方转账卡片 |

### 朋友圈 / 主页类

| 指令写法 | 用途 |
|---|---|
| `[进入朋友圈]` | 从发现页进朋友圈 |
| `[发朋友圈] 内容 \| 配图短名` | 我方发一条朋友圈（配图可空） |
| `[编辑朋友圈] [{"author":"我","text":"...","images":["短名"]}]` | 预置我的朋友圈动态；点赞/评论前必须先有动态 |
| `[点赞] 序号` | 点赞（留空=最后一条动态） |
| `[评论] 评论内容` | 评论最后一条动态 |
| `[闪回聊天] 回到=联系人` | 黑幕硬切回聊天页（剪辑感切镜）；`闪黑=否` 为裸硬切 |
| `[闪到对方朋友圈]` | 黑幕一帧闪进对方朋友圈（跳过资料页） |
| `[打开对方主页] 对方` | 点头像进对方资料页（`对方` 可空） |
| `[进入对方朋友圈]` | 资料页 → 对方朋友圈 |
| `[打开对方设置] 对方` | 资料页右上角「…」进联系人设置页 |
| `[加入黑名单] 确认=确定 \| 加载秒=1.4` | 拉黑全过程（`移出黑名单` 同款写法） |
| `[返回主页]` | 回聊天列表主页（收尾常用） |
| `[返回上一页]` | iOS 右滑推出 |
| `[切换Tab] 微信` | 底部 Tab（微信/通讯录/发现/我） |
| `[隐藏键盘]` | 收起键盘 |
| `[向下滚动] 300` / `[向上滚动] 300` | 朋友圈滚动像素 |
| `[滚动到] 底部` | 滚到顶/底（顶部/底部） |
| `[点开图片] 序号=2,3` | 点开朋友圈第 2 条的第 3 张图 |
| `[播放视频] 视频=/videos/x.mp4 \| 停留=5` | 点开朋友圈视频播放 |
| `[发送语音] 3` / `[对方语音] 5` | 语音消息（秒数决定波形长度） |
| `[撤回我的消息]` / `[对方撤回消息]` | 撤回最后一条 |
| `[等待] 2` | 画面停顿 2 秒 |
| `[编辑主页] [{"name":"某人","text":"最后一条"}]` | 重建聊天列表（历史块会自动生成，一般不用手写） |

### 其他动作（流程 / 预置类，聊天录屏较少出现，遇到时才用）

| 指令写法 | 用途 |
|---|---|
| `[移出黑名单] 确认=确定 \| 加载秒=1.4` | 移出黑名单全过程（含确认弹窗） |
| `[查看图片] 图片=短名 \| 停留=0.3 \| 焦点=0.35,0.4` | 单独查看放大一张图（历史块里标「打开动画」的图会自动生成此步） |
| `[编辑对方资料] {"name":"吴遂卿","wxid":"LSDH-WSQ","area":"广东 佛山","signature":"随心随性","avatar":"路径","cover":"路径","posts":[…]}` | 预置对方资料页+朋友圈内容（JSON 或 .json 路径） |
| `[编辑我的资料] {"name":"小明","avatar":"路径","bg":"路径","signature":"…"}` | 预置「我」的资料/头像/背景 |
| `[设置头像] 头像图片路径` | 换头像（设置页动画） |
| `[设置背景] 背景图片路径` | 换主页/朋友圈背景图 |
| `[修改昵称] 新昵称` | 资料页改名动画 |
| `[修改签名] 新签名` | 资料页改签名动画 |
| `[面板切换序列] [{"面板":"表情","停留":0.9},{"面板":"更多","停留":1.1},…]` | 按序列依次切底部面板（录制面板浏览过程） |
| `[打开转账面板]` → `[转账金额] 接收人=某人 \| 金额=50.00 \| 备注=xx \| 密码=123456` | 完整转账流程：进面板→填金额→输密码支付 |
| `[打开转账详情]` / `[接收转账]` / `[关闭转账详情]` | 转账详情页打开 / 点收款 / 关闭 |
| `[@成员] 昵称` | 群聊里 @ 某成员 |
| `[后台消息队列] [{"联系":"香儿","内容":"图片怎么发您"},…]` | 按队列依次推送多条后台消息 |
| `[应用场景] 场景=场景名 \| 数据=JSON(可空)` | 一键套用预设场景（人物+主页+消息） |
| `[编辑会话] 数据=JSON数组` | 重建指定会话的消息列表 |

---

## 五、视频 → 剧本转写规则

1. **忠实第一**：对白逐字照抄；连发几条就写几条，一条一行，不合并；不改写、不润色、不总结。
2. **打错回删**：本播放器的打字动画永远不会自动打错重打。视频里若出现「打错又退格重打」，转成 `[打字不发] 打错的字 | 停留秒` + `[删除文字] -1` + 正确的 `[我方打字]`。
3. **打字不发配对**：视频里打出字又删掉的 → `[打字不发] 内容 | 停留秒` 紧跟 `[删除文字] -1`。**[打字不发] 第 1 段就是视频里键盘上打出的那行字**（教学视频里通常是「给观众看的技巧字幕/博弈内容」，如 `礼貌开场`、`学以致用`——它不上屏发送，是给人看的，必须原样照抄）；它和随后真正发出去的话必然不同。
4. **边打字对方插话**（全片最关键的真实感来源，必须保留）：
   - 我打完字删掉了 → `[打字不发] 内容 | 停留 | 对方插话`
   - 我打完字发送了 → `[我方打字] 内容 | 对方插话`
   - 对方插多句照抄，用「；」隔开。
   - **插话支持全部消息格式**，视频里对方插的是什么类型就写什么（都是插话段的写法，不是独立动作行）：
     - 文字 → 直接写（文字里的 `[微笑]` 等按内嵌 emoji 渲染）
     - 表情贴纸 → `[对方表情] 素材短名`
     - 图片 → `[对方图片] 素材短名`
     - 链接卡片 → `[对方链接] 标题 | 封面 | 来源`
     - 3D 黄脸 emoji → `[对方emoji] 微笑`（名称/编号/随机）
     - 语音 → `[对方语音] 秒数`
     - 转账卡片 → `[对方转账] 金额 | 备注`
   - 示例：`[打字不发] 学以致用 | 1.0 | 你不准想太多；[对方表情] 勃然小怒猫咪`
5. **停留秒数**按视频实测时长写小数（0.5 / 1.0 / 1.5…），不要都写整数。
6. **对方正在输入**：视频里顶部出现才写，写实测秒数；没有就不加。
7. **等待**：只有画面静止明显超过 2 秒的地方才写 `[等待] 秒数`；平时消息间隙不加。
8. **时间条**照第 3.3 节，实时部分最多 3 处。
9. **图片/表情/封面**：转成 2~6 个字的素材短名（如「女生连衣裙」「害羞猫咪」「洱海日落」），并在剧本末尾附「素材对照清单」。
10. **emoji（重要）**：视频里「文字中带着小黄脸表情」的，用**内嵌写法**——把 `[名称]` 直接写进那条消息的文本里（如 `[我方打字] 太开心了[大笑] 你呢`、`[对方发消息] 是嘛[捂脸]`），不要拆成单独一行。只有「单独发出一个表情、没有文字」的才用 `[发送emoji]` / `[对方emoji]` 一行。名称只能用名称表里的名字（含别名：呲牙、红心、脸红、酷、睡觉、泪奔等）；看不清是哪个表情就写 `[对方emoji] 随机` 并记入存疑列表。
11. **人名、头像、昵称**全部照抄视频，不要改名；系统会自动对齐人物库。
12. **开场**：视频开头若是聊天列表 → 先写历史块再 `[打开聊天]`；直接在会话里 → 第一行就是 `[打开聊天]`。
13. **收尾**：视频结束画面停在主页 → 最后一行 `[返回主页]`；停在聊天页 → 可省略或按视频结尾写。
14. **禁止自创动作名**（如 `[为我方打字]`、`[我等打字]`、漏写右括号的 `[返回主页`）：这些行会被静默丢弃，剧本凭空少几行。

---

## 六、输出格式（三段，缺一不可）

**第 1 段 · 完整剧本**（纯文本，一行一条指令，可直接复制使用）

**第 2 段 · 素材对照清单**
| 短名 | 视频里的描述 | 出现时间点 | 谁 |
|---|---|---|---|

**第 3 段 · 存疑列表**：看不清、听不准、转换时拿不准的地方，逐条列出 + 说明，供人工修正。

---

## 七、输出前自查清单

- [ ] 每一行都以 `[动作表内的动作名]` 开头，括号成对
- [ ] `[历史会话]` 块在最前，且以 `[历史会话结束]` 收尾；`[打开聊天]` 在块外
- [ ] 历史块会话数 ≤ 9
- [ ] 每个 `[打字不发]` 之后，视频里删掉了就紧跟 `[删除文字] -1`
- [ ] `[打字不发]` 第 1 段=视频键盘上打出的字幕（照抄），≠ 后面发出去的话
- [ ] `[打字不发]` 的第 2 段是纯数字（停留）；插话里没有会被误判成秒数的内容
- [ ] 插话里的非文字消息都用了标记写法（`[对方表情]`/`[对方图片]`/`[对方链接]`/`[对方emoji]`/`[对方语音]`/`[对方转账]`），且素材短名出现在素材对照清单里
- [ ] 多参数动作全部用命名写法（`键=值 | 键=值`）
- [ ] 时间只写在行的最后一段；实时时间条 ≤ 3 处
- [ ] 图片/表情都是短名；emoji 都在名称表里
- [ ] 文字里带的小黄脸表情全部用了内嵌 `[名称]` 写法，没有错误地拆成单独一行
- [ ] 每条消息的时间戳照抄视频，没有整齐递增的编造时间
- [ ] 视频里没有的情节一个都没加（没补 [等待]、没补 [对方正在输入]）
- [ ] 最后一行符合视频结尾画面

---

## 附：一个最小完整示例

```
[历史会话]
[会话] 陆香儿
陆香儿：图片怎么发您 | 18:40
我：我教你 | 18:41
陆香儿：[图片] 洱海日落
[历史会话结束]

[打开聊天] 陆香儿
[打字不发] 你昨天拍的那张 | 1.2
[删除文字] -1
[我方打字] 你昨天那组照片是用什么拍的
[对方发消息] 手机随手拍的[微笑] | 18:52
[对方发消息] 没修图
[对方发图片] 图片=洱海日落 | 打开=是 | 停留=0.8
[等待] 1.5
[发送emoji] 爱心
[返回主页]
```

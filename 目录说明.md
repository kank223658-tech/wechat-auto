# 目录说明（清理后）

> 清理时间：2026-09-09；归档 `_archive_dev_20260909/` 已于 2026-09-12 确认后整目录删除。

## 怎么启动

| 想做什么 | 双击 / 命令 |
| --- | --- |
| **启动工作流编辑器（最常用）** | 双击根目录的 **`启动.bat`**（或 `start_all.bat`） |
| 打开独立场景编辑器 | `启动.bat scene` |
| 执行剧本并录制视频 | `启动.bat record` |
| 回滚最近一次候选备份 | `restore_candidate_backup.bat` |

`启动.bat` 只是转发到 `start_all.bat`，两个用哪个都一样。

## 根目录里哪些是"软件本体"

**入口**
- `启动.bat` / `start_all.bat` —— 一键启动
- `main.py` —— 录制/执行剧本主程序（体积大，是核心）
- `editor_server.py` —— 工作流编辑器服务（`http://localhost:8000`）
- `editor/manual.js` —— **软件主界面「📖 使用手册」的内容**（全部功能的书写格式与用法，离线可查）

> 📖 **使用手册怎么维护**：点编辑器顶部的「📖 使用手册」即可查看。
> 以后新增任何功能（动作、剧本指令、按钮、模式），都要同步往 `editor/manual.js` 里加一条：
> 找到对应分类的 `items` 数组，追加 `{ name, tag, syntax, desc, bullets, example }`；
> 全新分类就在 `categories` 里加一项 `{ id, title, icon, intro, items }`。
> 改完把文件顶部的 `updatedAt` 更新成当天日期即可，不需要动其它代码。

**配置与数据**
- `settings.json` —— DeepSeek API Key / 模型配置
- `workflow.json` —— 当前工作流（主数据）
- `tasks.json` —— 任务/历史记录
- `people.json`、`scene.json`、`peer_presets.json`、`reference_scripts.json`、`reference_workflow.json`
- `feedback.json`、`generate_history.json`、`link_library.json`、`create.db`
- `workflow_插话素材.json` —— 备用工作流
- `script*.txt`、`link_*.txt` —— 示例剧本

**代码与资源目录**
- `enhance/` —— 注入到微信页面的 CSS / JS（界面还原、输入法、表情等）
- `editor/` —— 编辑器前端资源
- `vue-WeChat/` —— 微信界面本体（`public/images/` 里的头像、表情等素材）
- `sounds/` —— 音效
- `tools/` —— 内置 Node 运行时
- `preview/` —— `preview_ui.py` 生成的界面预览图（输出）
- `videos/` —— 录制的成品视频（输出，体积大，按需清理）
- `参考图片/`、`微信图标/`、`苹果特效/`、`准备制作界面/` —— 素材目录
- `_runtime/` —— **运行时目录，程序会自动写入，不要删**

**辅助脚本**（一般不用管）
- `build_*.py`、`gen_freq_data.py` —— 生成词库数据（emoji_map.js 可直接手改）
- `measure_ui.py`、`preview_ui.py` —— 界面测量与预览
- `make_*.py`、`demo_*.py` —— 生成演示视频
- `create_store.py`、`sound_engine.py`、`script_generator.py`、`script_translator.py`

## 归档里有什么

`_archive_dev_20260909/` 里是 `_probe_*.py`、`_dbg_*`、`_tmp_*`、`_emoji_*`、各种 `*_frames/` 帧图目录、截图 PNG、`*.log`、`*.bak` 等一次性产物。
如果发现某个脚本其实还想用，直接从归档里拖回根目录即可。

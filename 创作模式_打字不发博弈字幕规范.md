# 「打字不发」博弈字幕 + 对方表情包插话 —— 需求规范（给创作模式）

> 本文档是需求的唯一权威口径。任何 AI 生成/转译剧本时，凡涉及 `[打字不发]`，一律按本文理解，**不得**把它当成"打了一段忘了发的聊天内容"。

## 一、需求本质（为什么存在）

这类剧本是**教学引流视频**，不是普通聊天记录。观众看的核心爽点有两个：

1. **键盘字幕**：视频主角一边聊，一边在键盘上打出「技巧点评」——如 `礼貌开场`、`学以致用`、`故意否定 引起注意`。这段字**永远不会发送**，打完就删掉。它是打给观众看的导演解说，是全片最精彩的部分。
2. **对方插话**：字幕打出来停住的那几秒，聊天窗口里**对方陆续发来消息**（文字或表情包），制造"我边讲解、对方边被拿捏"的博弈感。

所以 `[打字不发]` 的第 1 段**不是聊天内容**，是字幕；**不是失误**，是设计。

## 二、标准写法（三段语义）

```
[打字不发] 字幕内容 | 停留秒数 | 对方插话1；插话2
[删除文字] -1
```

| 段 | 含义 | 规则 |
|---|---|---|
| 第 1 段 | 键盘上打出的教学字幕/博弈内容 | 原样照抄，口语化短句即可（`礼貌开场`、`以退为进`）；**不发送**，上屏后停在输入框 |
| 第 2 段 | 停留秒数 | **纯数字才算停留**（`0.5`/`1.0`/`1.5`）；写了文字会被当成插话 |
| 第 3 段 | 对方边看你打字边发的插话 | 多句用「；」隔开；**支持表情包**：写 `[对方表情] 素材短名` |

执行时序（程序已实现）：完整打出字幕 → 停留 N 秒 → 对方按顺序逐条上屏（首条前带「对方正在输入…」）→ `[删除文字] -1` 清空 → 后续 `[我方打字]` 发真实话术。

## 三、插话支持全部消息格式（重点，勿丢失）

对方插话不只是文字——视频里对方插的是什么类型，就写什么。**全部写在插话段里**（不是独立动作行），按行首标记路由：

| 插话写法 | 渲染成 |
|---|---|
| 直接写文字（可内嵌 `[微笑]` 等小黄脸） | 文字气泡（内嵌 emoji 自动渲染） |
| `[对方表情] 素材短名`（兼容 `[表情]`/`[表情包]`/`[对方发表情]`） | 表情贴纸气泡 |
| `[对方图片] 素材短名`（兼容 `[图片]`/`[对方发图片]`） | 图片气泡 |
| `[对方链接] 标题 \| 封面 \| 来源`（兼容 `[链接]…`） | 链接卡片 |
| `[对方emoji] 微笑`（名称/编号/随机） | 3D 黄脸 emoji |
| `[对方语音] 5`（秒数） | 语音条 |
| `[对方转账] 520.00 \| 备注` | 转账卡片 |

示例（一条插话段里混排多种格式）：

```
[打字不发] 学以致用 | 1.0 | 你不准想太多；[对方表情] 勃然小怒猫咪；[对方图片] 洱海日落；[对方语音] 5
[删除文字] -1
```

- 所有素材短名必须是【可用素材】清单里真实存在的，并在剧本末尾素材对照清单里登记。
- `[我方打字]` 的插话段同样支持这套写法。

## 四、生成/转译时的硬性要求

1. `[打字不发]` 第 1 段必须保留且逐字照抄，**禁止**改写成聊天口吻、禁止删掉、禁止当成对方消息。
2. 每个展开的会话里 `[打字不发]` 要密集出现（连打好几次、删掉再打），紧跟 `[删除文字] -1`。
3. 插话只能回应"我已经发出去的上一条消息"，不写对方心理活动；按第三节的路由表选对插话格式，素材短名必须真实存在。
4. 字幕内容与随后真正发出去的话**必须不同**（本来就不是同一句话）。

## 五、给 Gemini 视频转写的对应规则

- 视频里键盘上打出的每一行字 → 第 1 段原样照抄。
- 视频里"打出→停住→对方回话→删掉" → `[打字不发] 字幕 | 实测停留 | 对方插话（按第三节路由表选格式）` + `[删除文字] -1`。
- 详细转写规则见 `视频转脚本_Gemini攻略.md` 第 3.2 / 4 / 5 节。

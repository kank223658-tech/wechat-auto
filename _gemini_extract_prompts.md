# Gemini 网页版 · 从真实视频提炼 [指令] 剧本（两段式提示词）

> ⚠️ **本文档已过时（2026-09-12）**：其中的动作表较旧，已被
> `视频转脚本_Gemini攻略.md`（按当前代码实况整理、可整篇粘贴）覆盖。
> 请使用新攻略，本文仅留作参考。

## 为什么是两个提示词，而不是一个

Gemini 网页版直接「看视频 → 写脚本」有三个必踩的坑：

1. **长视频后半段细节大量丢失**：超过几分钟，它会开始脑补没看清的字、把事件顺序搞乱。
2. **一次输出两件事必丢一件**：又要它仔细看、又要它同时学一套复杂格式，两边都做不好。
3. **时间戳最容易编**：它会随手填整齐的时间（19:01、19:02、19:03），而不是照抄视频里的。

所以拆成两步：**第一个提示词只让它「当眼睛」**，按时间轴逐条抄流水账，每条都带视频时间戳，方便你核对有没有编；**第二个提示词只让它「当翻译」**，把流水账转成 `[指令]` 格式。每步只干一件事，准确率完全不一样。

用法：
- 第 1 步：开新对话，上传视频 + 贴提示词 1 → 把它的回答保存下来，**抽查几条对不对**。
- 第 2 步：同一个对话里（或新开也行），贴提示词 2，然后把它上一轮的时间线原样贴在提示词 2 的末尾。
- 第 3 步：把产出的剧本丢进编辑器验证，图库里没有的素材名按它末尾的对照清单手动替换。

---

## 提示词 1（看视频 · 提取时间线）

~~~text
你是一名视频观察员。我上传了一段手机微信聊天界面的录屏视频。你的任务只有一件事：把视频里发生的每一件事按时间顺序逐条抄录下来。你不需要创作、不需要改写、不需要总结，只做忠实记录。

【总要求】
- 逐字照抄屏幕上出现的文字，一个字都不要改、不要润色。
- 每一条记录都必须带视频时间点（格式 mm:ss），并注明该事件持续了大约多少秒。
- 看不清的字就写「[看不清]」，绝对不要猜、不要编。宁可留空，不许编造。
- 宁可多记，不要漏记。哪怕看起来很小的动作（键盘弹出、轻点一下）也要记。

【第一部分：开场聊天列表】
如果视频开头展示了微信主页的聊天列表（哪怕只有一闪而过），请逐行抄录每一个会话的：
- 会话名（联系人昵称/备注）
- 最后一条消息的说话人和内容（逐字）
- 该会话右上角显示的时间（照抄，如 19:21）
- 顺序按列表从上到下
如果列表没出现过，写明「视频未展示聊天列表」。

【第二部分：进入会话后的事件流水】
进入聊天会话后，按时间顺序逐条记录，每条用下面的格式：

[mm:ss] 事件类型
- 内容：逐字照抄屏幕上的文字
- 说明：补充细节（见下方各类事件的说明）

需要记录的事件类型和要点：
1. 「我打字」：我方在输入框打字并发送了。抄输入框里打的全部文字 + 发送后气泡里的文字（两者都要）。如果打字过程中出现打错又退格重打，也要说明。
2. 「我打字未发」：我方打了字但停在输入框没有发送。抄输入框里的文字，并注明这些文字在输入框停留了几秒、之后是被删掉了还是被修改了。
3. 「删除」：我方按退格删除输入框里的字。注明是删了几个字还是全部清空。
4. 「我边打字对方插话」：我正在打字（键盘弹出、输入框有字），左侧突然弹出对方的消息气泡。这个非常关键，要记录：我当时输入框里的文字、对方插进来的消息内容（逐字）、对方一共插了几句、之后我是发送了还是删掉了。
5. 「对方正在输入」：顶部出现「对方正在输入…」，注明持续几秒。
6. 「对方发消息」：左侧白色气泡。逐字抄，一个气泡一条。
7. 「图片/表情/emoji」：描述图片或表情的内容（如「一张女生穿裙子的照片」「一只害羞的猫的表情包」），并注明是我方发的还是对方发的。
8. 「时间分隔条」：对话中间灰色的时间文字（如 19:21、昨天 23:40）。逐字照抄。
9. 「转账/链接/语音」等特殊消息：抄全部可见文字。
10. 「切换面板/点加号」：我方点击了输入框旁的加号或切换了底部面板。
11. 「手机状态栏变化」：时间、电量等明显变化（可以只在开头记一次）。
12. 「回主页/切到其他页面」：画面离开了当前聊天。
13. 「明显停顿」：画面静止超过 2 秒的地方，注明停了几秒。

【第三部分：结尾】
用 3~5 行回答：
- 这段视频是教学讲解类还是纯聊天记录类？
- 对方一共发了几条消息、我方发了几条、打字不发（未发送）出现了几次、对方插话出现了几次？
- 你看不清、拿不准的地方一共在哪些时间点？

再次强调：逐字照抄 + 每条带视频时间戳，看不清就写 [看不清]，绝不编造。
~~~


---

## 提示词 2（时间线 → 完整 [指令] 剧本）

~~~text
下面我会给你一份「视频事件时间线」，它来自对一段微信聊天录屏的逐条观察记录。你的任务是把它忠实转换成我这个播放器能执行的剧本，剧本格式是「一行一条指令」，形如 `[动作名] 参数`。你只做格式转换，不新增剧情、不改写对白——视频里有什么就写什么。

【动作表】（只能用这些动作名）
- [打开聊天] 联系人 —— 进入会话，实时对白的第一步
- [我方打字] 内容 —— 我方打字并发出
- [打字不发] 内容 | 停留秒数 —— 打出字停在输入框不发送
- [打字不发] 内容 | 停留秒数 | 对方插话一句 —— 打字不发期间对方插进来一句话
- [打字不发] 内容 | 停留秒数 | 插话1；插话2 —— 对方插多句用「；」隔开
- [删除文字] -1 —— 清空输入框，紧跟 [打字不发] 之后
- [我方打字] 正在打的这句 | 对方抢白一句 —— 我边打字对方插话，随后这句话发送出去
- [对方正在输入] 秒数
- [对方发消息] 内容
- [发送图片] 图片关键词 / [对方发图片] 图片关键词
- [发送表情] 表情名 / [对方表情] 表情名
- [发送emoji] emoji / [对方emoji] emoji
- [等待] 秒数
- [返回主页] 停留秒数
- [手机状态栏] 模式
- [转账] 金额 / [对方转账] 金额
- [我方发链接] 标题 | 图片 | 来源 / [对方发链接] 标题 | 图片 | 来源 | 时间

【历史会话块 —— 视频开头的聊天列表】
如果时间线第一部分抄录了聊天列表，把它原样转成：
```
[历史会话]
[会话] 联系人名
联系人名：消息内容 | 19:21
联系人名：消息内容
我：我方发过的消息
[会话] 下一个联系人
……
[历史会话结束]
```
- 时间照抄列表里显示的时间，写在「|」后面；没显示时间就不写。
- 我方在列表里说过的，写成「我：」开头。
- 图片消息写成 `联系人名：[图片] 描述短语`；链接写成 `联系人名：[对方发链接] 标题 | 封面 | 来源 | 时间`。
- [历史会话] 块只放历史消息；[打开聊天] 和实时对白必须写在历史块外面。

【转写规则】
1. 时间线里的「我打字」→ [我方打字] 内容；「我打字未发」→ [打字不发] 内容 | 停留秒数，紧跟 [删除文字] -1。
2. 时间线里的「我边打字对方插话」是全片最关键的真实感来源，必须保留：
   - 我打完字后删掉了 → [打字不发] 内容 | 停留 | 对方插话
   - 我打完字发送了 → [我方打字] 内容 | 对方插话
   - 对方插的多句照抄，用「；」隔开。
3. 「停留秒数」从时间线里的持续时长换算，写小数（如 0.5、1.0、1.5）。
4. 对方连续发几条就写几条 [对方发消息]，不要合并；我方连发也照抄连发。两条消息之间视频里有明显停顿才加 [等待]，平时不加。
5. 图片/表情：时间线里是描述短语，就转成 2~6 个字的素材短名（如「女生连衣裙」「害羞猫咪」），并在剧本末尾附一张「素材对照清单」（短名 → 时间线里的描述 → 出现的视频时间点），方便我去图库替换成真实文件。
6. 时间线里的「时间分隔条」→ 写在该条消息末尾 `| 19:21`。注意：整份剧本里这种实时时间分隔条最多 3 处，只保留换天、隔了很久这类关键节点的；如果视频里出现得太密，挑最关键的 3 处，其余丢掉。
7. 头像、昵称、联系人名全部照抄时间线，不要改名。
8. [打字不发] 的正文必须和它后面 [我方打字] 发出去的话不一样（视频里本来就不一样，你抄错才会一样）——转换完自查一遍。
9. 视频里没有的情节不要加：不要自己补「对方正在输入」、不要补 [等待]、不要改写任何一句对白。

【输出格式】
1. 完整剧本（[历史会话] 块在前，然后 [打开聊天] 开始的实时指令）。
2. 「素材对照清单」。
3. 「存疑列表」：时间线里写 [看不清]、或你转换时拿不准的地方，逐条列出并说明，方便我人工修正。

下面是时间线：

（在此粘贴提示词 1 的完整输出）
~~~

# 表情包功能说明书

本功能让「脚本」能像**发送图片**一样自如地**发表情包**，并自动区分图片与表情包（两者气泡尺寸不同：图片是整图等比缩放、约 196×196；表情包是小尺寸贴纸、固定 78×78，无白底直接上屏）。

离线解析时，脚本里所有「我方发表情」会自动合成一个**表情包库**（去重，通常 3 张左右），注入到前端的表情面板里；这些表情包会被**随机放到面板格子的几个位置**。对方发表情不会进我的表情包库。

---

## 一、怎么引用表情包

表情包和图片一样，本质是一张图片文件。引用方式有三种，任选其一：

| 写法 | 含义 | 例子 |
|------|------|------|
| **完整路径** | 直接指定图片 | `[发送表情] /images/avatar/赵本山表情包_20260903_193137_046.jpg` |
| **文件名** | 省略 `/images/...` 前缀 | `[发送表情] 认可表情包_20260905_175240_649.jpg` |
| **短名字** | 只写关键名字，系统自动去图片目录里匹配同名文件 | `[发送表情] 赵本山`、`[发送表情] 认可表情包` |
| `随机` | 从表情包库里随机挑一张发出 | `[发送表情] 随机` |

短名字匹配规则：先按图片文件名的「去扩展名部分」精确匹配，再按「名字是否包含在文件名里」模糊匹配。图片放在 `vue-WeChat/public/images/avatar/`（个人头像）或 `images/emoji/`（用户自传表情图）里都能被找到。

> 运行前把你要用的表情图放到上面的目录（或直接丢进项目根目录的 `表情包图片` 文件夹，程序会自动同步到 `/images/myemoji/`），然后在脚本里用路径或短名字引用即可。系统会自动把它们放进「我方的表情包库」。

---

## 二、表情相关的指令（文本剧本格式）

文本剧本是一行一条指令：`[指令] 参数`。`#` 开头是注释，空行忽略。

### 我方发表情（会进表情包库）

```
[发送表情] 图片路径或短名字
[发送表情] 赵本山 | 18:22                          ← 带时间分隔条
[发送表情] 随机                                     ← 从库里随机挑一张
```

别名字（写法更随意也能识别）：`发表情`、`发一个表情`、`发贴纸`、`发送表情包`、`我方发表情`、`我方发送表情`。

参数名：`表情`。可以写 `表情`，兼容写 `图片`。

### 对方发表情（直接上屏，不进我的库）

```
[对方表情] 哭泣猫咪
[对方表情] /images/avatar/哭泣猫咪_20260905_175009_399.jpg
```

别名：`对方发表情`、`对方表情包`、`对方发送表情`、`对面发表情`。

### 对方后台发表情（给「没打开的会话」投递，刷新主页预览/角标）

```
[对方后台发表情] 梓康群 | 好的表情包 | 18:40
```

格式：`联系人 | 表情 | 时间`。联系人必须是首页会话列表里已有的名字（或用 `[编辑主页]` 先加进去）。这条不会进我的表情包库。

---

## 三、时间标注（和图片完全一致）

只要是能产生「会话内消息」的动作，都支持在参数末尾写时间，前面用 ` | ` 隔开：

```
[发送表情] 赵本山 | 18:22            → 18:22（今天）
[发送表情] 赵本山 | 昨天 23:52      → 昨天 23:52
[对方表情] 哭泣猫咪 | 昨天 18:40
[对方后台发表情] 梓康群 | 好的表情包 | 18:40
```

支持的写法（和图片功能同一套规则）：`HH:MM`、`昨天 HH:MM`、`星期/周X HH:MM`、`M月D日 HH:MM`、`N分钟前`、`N小时前`、`N天前`、毫秒时间戳。

时间必须写在**最后**一个 ` | ` 分段，其它 ` | ` 分段（联系人/表情等）不受影响。

---

## 四、图片 vs 表情包（尺寸区分）

解析时按**动作名**自动路由到不同气泡：

| 动作 | 气泡类型 | 尺寸 | 说明 |
|------|----------|------|------|
| `发送图片` / `对方发图片` | `msg-image` | 面积恒定等比缩放，约 **196×196**（横图更宽、竖图更高） | 整张图 |
| `发送表情` / `对方表情` | `msg-emoji` | 固定 **78×78**，无气泡底，直接显示贴纸 | 小尺寸表情包 |

所以：

- 要发**一整张大图** → 用 `[发送图片]`。
- 要发**小尺寸表情包** → 用 `[发送表情]`。

不要写混：`[发送图片]` 不能改表情尺寸，`[发送表情]` 也不应该用来发大图。

---

## 五、workflow.json（JSON 格式）

脚本也可以用 JSON 编写，动作与参数如下：

```json
{
  "steps": [
    { "action": "打开聊天", "params": { "联系人": "陆香儿" } },
    { "action": "发送表情", "params": { "表情": "/images/avatar/赵本山表情包_20260903_193137_046.jpg", "时间": "18:22" } },
    { "action": "对方表情", "params": { "表情": "认可表情包" } },
    { "action": "对方后台发表情", "params": { "联系人": "梓康群", "表情": "好的表情包", "时间": "18:40" } }
  ]
}
```

参数名与文本剧本一一对应：`表情`、`联系人`、`时间`。

---

## 六、表情包库是怎么来的

离线解析时（`python main.py` 加载脚本/工作流的阶段），系统自动做三件事：

1. **扫**所有「我方发表情」动作（含别名）。
2. 把里面的短名字**解析成真实图片路径**，然后**去重**（相同表情只算一张，通常 3 张左右）。
3. 把这份库注入前端：表情面板「添加的单个表情」网格显示的就是这些图，并且**随机放到几个格子位置**。

对方发表情（`对方表情`/`对方后台发表情`）**不会**进我的表情包库。

### 最简单：把图丢进「表情包图片」文件夹（无需登记）

不写任何清单也能让表情包一直出现在表情面板里：把表情包图片丢进项目根目录的 **`表情包图片`** 文件夹（`F:\weixin-auto\表情包图片\`）。程序每次启动会自动：

1. **扫描**这个文件夹里所有图片（`.png / .jpg / .jpeg / .webp / .gif / .bmp`）；
2. 把它们**同步**到前端公开目录 `vue-WeChat/public/images/myemoji/`（同名图刷新、源目录删掉的旧图自动清理，不会残留旧表情）；
3. 注入表情面板——打开应用这几个表情包就自动出现；发送时点选，或在剧本里用 `[发送表情] 文件名` 都能成功。

> 首次运行若还没有这个文件夹，会**自动创建**并在启动日志提示「把表情包图片丢进这个文件夹」。里面已有图片时会提示几张、已同步。
>
> 想在剧本里引用它，用**文件名**即可（可省略 `/images/...` 前缀），例如 `[发送表情] 猫咪蹭头.png`，或写文件名里包含的关键词 `[发送表情] 猫咪蹭头`。

### 可选：用手动清单 `emoji_library.json`

如果你想手动指定表情包库（不依赖剧本里的引用），可以在项目根目录建 `emoji_library.json`：

```json
[
  { "name": "赵本山", "url": "/images/avatar/赵本山表情包_20260903_193137_046.jpg" },
  { "name": "认可",   "url": "/images/avatar/认可表情包_20260905_175240_649.jpg" }
]
```

格式：JSON 数组，每项可以是：

- 字符串：`"/images/avatar/xxx.jpg"`；
- 对象：`{ "名字": "...", "url": "/images/..." }`（`name`/`url` 键名也可）。

清单里列的图会**永远**进表情包库，并和剧本里引用到的我方表情合并去重。没有这个文件时，纯靠剧本自动收集。

> **引用不到图时的行为**：如果 `[发送表情]` 里的「短名字 / 文件名」在图片目录里**找不到对应文件**，系统**不会**把它当作黑图塞进面板或发出去，而是**回退成默认表情**，并在启动日志里打印一条提示，列出哪些名字没找到、以及该把图放到哪个目录。所以：
> - 想让某个表情包真正显示，先把它放进 `vue-WeChat/public/images/avatar/` 或 `images/emoji/`，再用它的**文件名（可省略 `/images/...` 前缀）**或**文件名里包含的关键词**去引用；
> - 如果日志提示「没找到对应图片文件」，就说明这张图还没放进去（或名字对不上），按上面提示补图即可。

---

## 七、运行时：先开键盘再开表情包（动作连贯）

目前前端没有「键盘没弹开就点表情」的入口，所以当脚本**在未打开键盘的情况下**直接发表情时，运行时会：

1. 先弹出手机键盘（等它上推动画播完）；
2. 紧接着无缝切换到表情面板（面板滑入、自动高亮目标表情、收起、上屏）。

中间**不加额外停顿**，动作连贯。键盘本身已是弹出态则直接切面板。

---

## 八、常见示例

```text
# 会话里连续发图片和表情，图片是大图、表情是小贴纸
[打开聊天] 陆香儿
[对方发消息] 今天去哪玩
[发送图片] /images/album/baiqian/baiqian01.jpeg | 18:22
[发送表情] 赵本山 | 18:22
[发送表情] 随机 | 昨天 23:52
[对方表情] 哭泣猫咪
[对方后台发表情] 梓康群 | 好的表情包 | 18:40
[我方打字] 哈哈，改天一起
```

解析结果（`[发送表情]` 带时间、短名字被解析成真实路径、`随机` 保留为随机；对方表情不进库）：
直接运行 `python main.py`（读取 `script.txt`）或 `python main.py --workflow work.json` 即可。

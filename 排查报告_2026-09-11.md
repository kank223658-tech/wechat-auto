# weixin-auto 使用问题排查报告（2026-09-11）

> 本次只做检查，没有改动任何软件文件。按「影响使用的程度」从高到低排列。
>
> **✅ 2026-09-11 更新：上面说的 1~7 号问题已全部修复生效（另附带修了 12、13 号），
> 剩下未动的只有：#8（生成纠错重复拼提示词）、#9~#11（死文件/动作定义/根目录整理）。**

## 一、健康检查结果（都正常）

- 核心代码（main.py、editor_server.py、生成流水线等 5 个文件）编译全部通过，没有损坏。
- 渲染服务（8001）和编辑器后端（8000）当前都在线，工作正常。
- 前端页面服务（8080）当前没开——这个平时按需启动，不是故障。
- 创作模式体检报告已重跑（`_diag_gen_report.md`）：旧的 198 个历史问题都已有对应处置，等下一次真实生成即可验证效果。

## 二、会踩到的坑（建议优先修，共 5 个）

### 1. 程序崩了没有任何提示 ⭐最影响体验
点「运行」后如果程序在启动瞬间崩掉，界面上不会报错，只会一直显示「就绪」，
看起来像是「没点上」。真实原因藏在运行日志里，用户很难发现。
**修法：崩溃时在页面上弹红色提示，并直接显示日志最后几行。**

### 2. 不该连的画面连接在空转
「实时画面」面板每 0.35 秒就尝试连一次手机画面；但只有「编辑模式」才有画面。
如果当前只是普通运行，它会对着一个不存在的服务空转，永远显示「正在连接」。
**修法：不是编辑模式时，直接提示「请先进入编辑模式」，别空转。**

### 3. 「AI 转译」长剧本容易超时失败
同一个 AI 接口，「AI 生成」设了 10 分钟超时还会自动重试（很稳），
但「AI 转译」只有 60 秒且不重试——长剧本经常转译到一半失败。
**修法：让转译用和生成一样的超时和重试设置。**

### 4. 8080 端口被别的软件占用时，预览会静默坏掉
如果电脑上有其他软件占了 8080 端口，左侧手机预览会一片空白，
且全程没有任何报错，用户完全不知道为什么。
**修法：端口被占时明确报错提示「请释放 8080 端口」。**

### 5. 首次启动体验差
每次启动都重复做一遍安装检查（其实装过就不用查）；缺组件或编译失败时
预览空白但没有任何提示。
**修法：装过就跳过检查；缺东西时在页面上明确提示。**

## 三、速度优化点（中等收益，共 3 个）

### 6. 图片库每次都全盘重新扫描
打开图片库、上传一张图、保存设置……每次都把整个图片目录重新扫一遍。
图库越大越慢。**修法：加一个几秒钟的缓存，改动了才重新扫。**

### 7. 设置文件被反复读取
状态查询、AI 配置、转译、生成……每次请求都重新读一遍 settings.json。
单次不慢，但和第 6 条叠在一起会拖累整体。**修法：读一次缓存住，保存时刷新。**

### 8. AI 多轮纠错时重复拼长提示词
纠错循环里，人物、参考剧本这些不变的内容每一轮都重新拼一遍。
**修法：循环外先拼好，每轮只换变化的部分。**

## 四、代码整洁问题（不影响使用，共 5 个）

9. `reference_scripts.json`（26KB）已经没人读了——真正用的参考库在 SQLite 里，文件可删。
10. 动作定义在 3 个地方各存一份，改动作时容易漏改、两边对不上。
11. 根目录堆了 100 多个 `_bak*`、`_diag*`、`_check*` 等临时/备份文件（含 1.5MB 的历史记录），容易点错、仓库变肥。建议归进 `_scratch/` 文件夹。
12. `/api/runlog` 接口解析参数的方式太脆弱，参数稍不对就内部报错。
13. 实时画面即使已经有图了也持续整帧刷新，用久了给渲染进程持续压力。**修法：连续失败几次就降速或暂停。**

## 五、建议的修复顺序

1. **先修 #1（崩溃无提示）+ #4（端口占用静默坏）** —— 决定第一次用能不能跑通。
2. 再修 #2（空转）+ #3（转译超时）—— 日常使用体感。
3. 然后做 #6（图库缓存）—— 性能收益最大的一项。
4. 整洁问题可以攒着一次清理。

# 旧版遗留扫描报告

> 扫描时间：2026-09-11 16:40
> 扫描方式：全仓库静态引用分析（550 个源码文件建引用图）+ 逐项人工核实 + 文件时间戳活性检测
> **本次只读扫描，未删除、未修改任何文件**

---

## ⚠️ 先说一个必须知道的发现

**此刻有另一个会话正在这个项目里干活。** 我查了文件时间戳，下面这些目录**最近 10 分钟内还在被写入**：

| 目录 | 最后写入 | 状态 |
| --- | --- | --- |
| `_shot/` | 16:38:40 | 🔥 正在写 |
| `videos/` | 16:38:14 | 🔥 正在写（有新录制） |
| `_mic/` | 16:37:38 | 🔥 正在写 |
| `_numref/` | 16:36:58 | 🔥 正在写 |
| `_emoji_cut/` | 16:32:40 | 🔥 正在写（表情素材修复） |
| `_panel_switch_out/` | 16:27:06 | ⏱ 刚用过 |
| `_ek/` `_ekold/` | 15:54 / 15:53 | ⏱ 刚用过 |
| `_fix7/` `_vg/` | 15:51 / 15:49 | ⏱ 刚用过 |

**结论：这几个目录现在不能删。** 等另一个会话忙完、或者你确认它已经不用了，再动它们。
我把它们从「确定没用」里挪出来，单独归到 **2.2B「暂缓」**。

---

## 一、结论速览

| 分类 | 内容 | 空间 |
| --- | --- | --- |
| ✅ **已执行清理** | 归档 + 调试脚本 + 长期未动调试产物 + 日志/缓存（A+B+C 层） | **已释放 3.7 GB** |
| 🟠 **暂缓（有并发会话在用）** | `_shot/` `_mic/` `_numref/` `_emoji_cut/` `_ek*/` `_vg/` `_fix7/` `_panel_switch_out/` | 约 210 MB |
| 🟡 **存疑** | 旧引擎备份、试验工程、演示配置、失效设定 | 约 500 MB |
| 🟢 **新发现：老画面遗留** | vue-WeChat 上游开源素材 + 空目录 + 悬空引用 | 见**第八节专章** |
| 🔴 **千万别动** | 软件本体、当前配置、在用素材 | — |

**两个正在生效的问题**（都需要你决定，我都没动）：

1. **DeepSeek 模型已下线** —— AI 生成剧本功能现在是坏的（第五节的实测证据）
2. **一批图片引用悬空** —— `images/ref/` 和 `images/album/` 被清空了，但代码和
   `scene.json` 还在指它们，会显示空白（第八节 8.3）

---

### ✅ 本轮已执行（2026-09-11 17:10 全部完成）

**共释放约 3.76 GB**（工作区 8.3 GB → 4.6 GB，其中 `videos/` 成品视频仍占 2.8 GB）：

| 批次 | 内容 | 释放 |
| --- | --- | --- |
| ① A 层 | 日志 / enhance 临时 dump / 设计草稿备份 | 34.7 MB |
| ① B 层 | 118 个一次性调试脚本 + 测试 JSON + 长期未动的探针产物目录 | 514.6 MB |
| ① C 层 | `_archive_dev_20260909/` 上一轮归档 | 3153.5 MB |
| ② 老画面 | 上游宣传素材 + 空目录 + avatar 重复/误传文件（40 项） | 16.5 MB |
| ③ 悬空引用 | 4 个文件、56 处路径改为真实存在的图片 | — |
| ④ 存疑项 | `engine_poc/` `enhance/_backup/` `enhance/rime/` `复刻_界面/` `preview/` 内容 + 21 个演示测试配置（51 项） | 43.7 MB |

**执行时加了三道保险**：

1. **年龄过滤** —— 60 分钟内被改动过的一律跳过（另一个会话正在跑脚本），
   最终因太新跳过了 40 项，包括它刚建的 `_pv_*.py`、`_smoke_gallery2.py` 等
2. **备忘保护** —— 凡在项目备忘里登记过的脚本一律保留（63 个），
   并做**正向依赖闭包**，连带保住被它们引用的公共库
3. **删除前语法/引用校验** —— 核心模块 `py_compile`、JSON 有效性、关键文件在位检查

**已保留的常用排障工具**：`_check_blank.py`、`_check_scrollfx.py`、`_cmp_frames.py`、
`_state_scan.py`、`_probe_num.py`、`_probe_mic.py`、`_vg_track4.py`、`_vg_final.py`、
`_verify_panel_video.py`、`_probe_prewarm.py`、`_scan_*.py` 等 63 个 —— 全部在位。

**⚠️ 这次清理带来两个功能变化，你需要知道**：

1. **「候选回滚」功能失效** —— `enhance/_backup/` 里的 12 份快照已删，
   `restore_candidate_backup.bat` 不再有源可回滚（那些快照是 09-04/05 的，本就已严重过时）
2. **`build_pinyin_dict.py` 少了明月拼音数据源** —— `enhance/rime/` 已删，
   重新生成 `enhance/pinyin_data.js` 时会少一个多音字来源（主程序不受影响，仍走雾凇词库）

**我特意保留没删的两个小文件**（各几十 KB，删了省不了空间但会丢功能）：

- `enhance/cjk_freq_extra.json` —— `build_pinyin_dict.py` 的输入数据
- `enhance/emoji_map.json` —— `fetch_apple_emoji.py` 的输入数据

（还有 `workflow_插话素材.json`、`script_moments.txt`、`script_peer_moments.txt` 这 3 个
看着像你自己攒的素材剧本，我没动，等你确认。）

---

## 二、🟢 确定没用（可以删）

### 2.1 上一轮归档目录 —— 3.1 GB

| 路径 | 说明 |
| --- | --- |
| `_archive_dev_20260909/` | 8857 个文件。`目录说明.md` 里你自己写过：「确认软件能正常跑之后，直接删掉这个文件夹即可回收空间」 |

这是全仓库最大的一块，且与当前工作区完全隔离、无任何引用。
里面有 `main.py.bak_20260905`、`workflow.json.bak*`、`chat_exact.css.bak`、
`pinyin_data.js.bak_timing` 等历史备份。

### 2.2A 长期未动的调试产物目录 —— 约 495 MB

时间戳都是 **今天早上或更早**，且与代码零引用，可安全删：

| 目录 | 大小 | 最后写入 |
| --- | --- | --- |
| `_fs/` | 364 MB | 09-11 06:22 |
| `_ref_tf/` | 53 MB | 09-11 07:08 |
| `_ref3/` `_ref3b~e/` | 约 65 MB | 09-11 13:50 |
| `_ref30/` | 5.1 MB | 09-11 14:17 |
| `_vg60/` | 3.8 MB | 09-11 14:17 |
| `_ref_crop/` | 9.3 MB | 09-11 01:15 |
| `_ref_frames/` | 8.7 MB | 09-10 21:17 |
| `_ref_tf2/` | 5.9 MB | 09-11 13:38 |
| `_block_frames/` `_block_verify/` | 3.4 MB | 09-11 02:52 |
| `_ps_probe/` | 352 KB | 09-11 13:48 |

> 这些都是探针帧图、对比图、模板匹配中间产物，删掉不影响软件运行。

### 2.2B 🟠 暂缓：正在被另一个会话使用的目录 —— 约 210 MB

**不要现在删**，等那个会话结束：

| 目录 | 大小 | 最后写入 |
| --- | --- | --- |
| `_shot/` | 36 MB | 16:38 🔥 |
| `_emoji_cut/` | 13 MB | 16:32 🔥 |
| `_ek/` | 14 MB | 15:54 |
| `_ekold/` | 12 MB | 15:53 |
| `_numref/` | 11 MB | 16:36 🔥 |
| `_mic/` | 64 KB | 16:37 🔥 |
| `_fix7/` | 66 MB | 15:51 |
| `_vg/` | 40 MB | 15:49 |
| `_panel_switch_out/` | 2.1 MB | 16:27 |

> 我看了今天的项目日志，另一个会话正在做「e02 捂脸表情修复」，`_emoji_cut/` 里的
> `official/`、`backup_jpeg_cut/` 都是它的工作素材。**建议等它确认完工后再清。**

### 2.3 根目录一次性脚本与测试文件 —— 约 11 MB

- **118 个 `_*.py` 调试脚本**：`_probe_*.py`、`_dbg_*.py`、`_pv_*.py`、`_ref3_*.py`、
  `_vg_*.py`、`_check_*.py`、`_measure_*.py`、`_verify_*.py` 等，全部零引用
- **测试工作流 JSON**：`_demo_*_wf.json`、`_test_*_wf.json`、`_verify_wf.json`、
  `_replica_panel_wf.json`、`_probe_scene.json`、`_shot_scene*.json`
- **截图/录屏**：`_*.png`、`_ab_anim_cmp.mp4`、`_em_start.txt`
- **本次扫描产生的临时文件**：`_scan_refs*.py`、`_scan_real.py`、`_scan_actions.py`、
  `_scan_refs_out.json`

> ⚠️ 其中约 30 个（`_shot.py`、`_probe_mic.py`、`_check_blank.py`、`_vg_track4.py` 等）
> 在项目长期备忘里被登记为「可复用排障工具」。**要不要保留这批，请你定。**

### 2.4 日志与缓存 —— 约 14 MB

| 路径 | 大小 | 说明 |
| --- | --- | --- |
| `.cursor/debug-189770.log` | 7.9 MB | Cursor 调试日志 |
| `vue-WeChat/*.log`（7 个） | 3.5 MB | dev-server / vue-dev / restarted_dev 等 |
| `__pycache__/` | 1.9 MB | Python 字节码缓存，会自动重建 |
| `_runtime/t26*.json` + `.log` | 1.2 MB | 09-05～09-09 的历史任务记录 |
| `editor_edit.log`、`editor_run.log` | 4 KB | 编辑器运行日志 |

> ⚠️ `_runtime/` **目录本身要保留**（`editor_server.py` 会往里面写任务工作目录），
> 只清里面的历史任务文件。

### 2.5 enhance 临时大文件 —— 23 MB

- `enhance/_tmp_p.json`（8.8 MB）、`enhance/_tmp_wx.json`（14 MB）
  两个临时抓取 dump，已在 `.gitignore` 排除，无任何引用

### 2.6 编辑器设计草稿备份 —— 0.8 MB

- `editor/_redesign/scene.html.bak_prepv`、`.bak_fit`、`.bak_sync`（各约 260 KB）

---

## 三、🟡 存疑（需要你拍板）

### 3.1 `engine_poc/` —— 31 MB（含内嵌 .git 仓库）

- 内容是输入法引擎「试验工程」（`my_rime/` 整个 git clone + `rime_ice.schema.yaml` + `runner/`）
- **现状**：正式方案早已落地为 `vue-WeChat/public/engine/`（WASM）+ `enhance/rime-ice/`（雾凇词库）
- **我的判断**：试验工程，已被取代 → 倾向可删
- **风险**：以后想回看当时的选型对比就没了

### 3.2 `enhance/_backup/` —— 12.4 MB

- 12 个 `20260904_HHMMSS` 时间戳目录，各含一份完整的 `main.py` / `keyboard.py` 快照
- 是 `restore_candidate_backup.bat` 的回滚源
- **我的判断**：09-04 的快照，距今 7 天、代码已改动极大，回滚价值低
- **风险**：删了就没有「候选回滚」这个功能了

### 3.3 `enhance/rime/` —— 2.2 MB（明月拼音 luna_pinyin）

- 只有 `build_pinyin_dict.py` / `build_rime_dict.py` 把它当**可选输入**读
- 主程序实际用 `enhance/rime-ice/wusong_data.json`（雾凇）
- **我的判断**：上一代词库，已被雾凇取代 → 倾向可删
- **风险**：重新生成 `pinyin_data.js` 时少了这个多音字数据源

### 3.4 其余 enhance 小文件 —— 约 32 KB

| 文件 | 大小 | 只被谁用 |
| --- | --- | --- |
| `enhance/cjk_freq_extra.json` | 23 KB | `build_pinyin_dict.py` / `gen_freq_data.py`（生成脚本） |
| `enhance/emoji_map.json` | 8.7 KB | `fetch_apple_emoji.py`（线上走 `emoji_map.js`） |
| `enhance/_ref_metrics/` | 0.1 MB | 无人引用 |

### 3.5 `editor/_redesign/` 其余草稿 —— 约 0.9 MB

- `_ux_*.py`、`_live_stage*.py`、`_pv_*.py`、`_restyle2.py`、`index_new.css` 等，
  是 UI 改版时的一次性生成/校验脚本
- **我的判断**：改版已完成 → 倾向可删

### 3.6 根目录演示与测试配置 —— 约 45 KB

逐个核实过引用数，**全部为 0**：

```
complex_demo.json        demo_full_ops.json      demo_transfer.json
demo_workflow.json       workflow_sparse.json    workflow_test.json
workflow_delcheck.json   smoke_test.json         verify_ui.json
diff_keyboard.txt        script_demo_fix.txt     link_workflow.json
link_video_workflow.json link_video_script.txt   link_demo.txt
demo_multi.py            demo_chat_image.py      make_demo.py
make_pullpush_video.py   measure_ui.py           fetch_apple_emoji.py
```

**但下面 3 个要你亲自看一眼再决定**（看着像你自己攒的素材）：

| 文件 | 大小 | 说明 |
| --- | --- | --- |
| `workflow_插话素材.json` | 25 KB | 目录说明里叫「备用工作流」 |
| `script_moments.txt` | 3.5 KB | 朋友圈素材剧本 |
| `script_peer_moments.txt` | 3.7 KB | 对方朋友圈素材剧本 |

### 3.7 `复刻_界面/` —— 2.3 MB

- `我.html`、`通讯录.html` + `assets/`，9-11 早期「手写复刻界面」的尝试
- **现状**：正式实现走 `vue-WeChat` + `enhance/`，这份已被取代

### 3.8 `preview/*.png` —— 4.3 MB

- `preview_ui.py` 生成的界面预览图（输出目录，可随时重新生成）

### 3.9 `videos/` —— 2.8 GB ⚠️

- 录制的**成品视频**，最大单文件 180 MB；最新一支是 16:38 刚录的
- 这是你的工作产出，**我不建议代你清理**，但你可以自己清旧的那批
  （最老 09-01，最新 09-11；按需保留）

---

## 四、🔴 补充：动作库一致性核查结果（结论是"没问题"）

特意查了这一项，因为它最容易藏旧版遗留：

- 服务端 `editor_server.py` 定义 **60 个动作**
- 主程序 `main.py` 派发 **80 个动作名**
- 多出来的 20 个（`打字`、`发表情`、`回到聊天主页`、`转账上屏`、`应用场景`…）

✅ 经逐个核实，**全部是有意保留的别名兼容层**（`main.py` 里的 `ACTION_ALIASES` 表），
不是死代码。它的作用是让老剧本、旧转译结果还能跑。**建议保留。**

❌ 反过来，「服务端定义了但主程序找不到实现」的动作：**0 个**。这块是干净的。

---

## 五、🔴 最该先修的一条：DeepSeek 模型已下线

这不是文件清理问题，是**正在生效的配置故障**。

**实测证据**（刚查的官方接口）：

```
GET https://api.deepseek.com/models
→ 当前只有两个可用模型：deepseek-flash、deepseek-v4-pro
```

**你的配置里写的是**：

| 位置 | 当前值 |
| --- | --- |
| `settings.json` | `deepseek-v4.1-flash-expires-on-0910` ← 名字里就写着 09-10 过期，今天 09-11 |
| `settings.example.json` | 同上 |
| `editor/index.html:2810` | 下拉框第一个选项（还标注着「推荐」） |
| `editor/index.html:4497` | 打开编辑器时的默认回填值 |
| `editor/index.html:4508` | 没选模型时的兜底值 |
| `editor/manual.js:711` | 手册写的是「模型默认 deepseek-v4-flash」← 和代码都不一致 |

下拉框里另外三个 `deepseek-v4-flash` / `deepseek-chat` / `deepseek-reasoner`，
也都不在当前可用列表里。

**影响**：AI 生成剧本 / 剧本转译功能会直接报错。

**建议**：默认模型改成 `deepseek-flash` 或 `deepseek-v4-pro`，同步改上面 4 处。
（`generate_history.json` 里大量历史记录写着旧模型名，那是历史数据，不用动。）

---

## 六、🔴 千万别动（白名单）

| 类别 | 路径 |
| --- | --- |
| **入口** | `启动.bat`、`start_all.bat`、`main.py`、`editor_server.py`、`preview_ui.py` |
| **核心配置** | `settings.json`、`workflow.json`、`tasks.json`、`scene.json`、`people.json`、`peer_presets.json` |
| **数据文件** | `reference_scripts.json`、`reference_workflow.json`、`feedback.json`、`generate_history.json`、`create.db`、`link_library.json` |
| **代码** | `editor/`（除 `_redesign` 草稿）、`enhance/`（除 `_backup`/`_tmp_*`/`rime`）、`vue-WeChat/src/`、`vue-WeChat/public/images/` |
| **引擎** | `vue-WeChat/public/engine/`（含 `worker_local.js`，**在用**）、`enhance/rime-ice/`、`enhance/pinyin_data.js` |
| **音效素材** | `sounds/`、`苹果音效/`（`背景音乐.mp3` 被 main.py 引用） |
| **图片素材** | `参考图片/`、`微信图标/`、`准备制作界面/`（`prepend_intro.py` 被 main.py 调用）、`vue-WeChat/public/images/` |
| **运行时** | `_runtime/` **目录本身** |
| **辅助模块** | `create_store.py`、`script_generator.py`、`script_translator.py`、`sound_engine.py` |

---

## 七、建议执行顺序

1. **先修模型配置**（第五节）—— 唯一会影响正常使用的问题
2. **删 2.4 / 2.5 / 2.6**（日志、临时 dump、设计草稿）—— 零风险，约 38 MB
3. **删 2.3**（118 个调试脚本 + 测试 JSON）—— 零引用，约 11 MB
4. **删 2.2A**（495 MB 长期未动的调试产物）
5. **删 2.1**（3.1 GB 归档）—— 删前建议先跑一次 `启动.bat record` 确认软件正常
6. **等并发会话结束后再删 2.2B**
7. **逐项处理第三节存疑项** —— 建议一个一个来

---

## 八、专项：vue-WeChat 老开源底子的「旧画面」遗留

> ✅ **本节 8.1 / 8.2 / 8.5 已执行清理（40 项，16.5 MB）；8.3 的悬空引用已修复（56 处）。
> 修好后的状态：`store.js` / `contacts.js` / `scene.json` / `reference_workflow.json`
> 已 0 悬空引用，JSON 与 JS 语法均校验通过。原文件备份为 `*.bak_dangling`。**
>
> 8.4 的存疑项（`welcome.jpg`、`qrcode.jpg`、老版微信图标组）**未动**；
> 8.3 之外的其余悬空引用（`main.py` 兜底默认头像 36 处、上游 `.vue` 组件里的三国人物头像、
> `editor/*` 里的示例路径）也**未动**，见第 8.7 节。

你说「之前遗留下来很多旧画面的东西和截图好像都没有被清理」——我把这层单独挖了一遍。
`vue-WeChat/public/images/` 共 51 MB / 500+ 文件，其中相当一部分是**上游开源项目自己的东西**。

### 8.1 🟢 确定没用：上游开源项目的宣传/演示素材（0 引用，约 1.6 MB）

这些和你的软件毫无关系，是上游项目 README / 文档用的图：

| 文件 | 大小 | 是什么 |
| --- | --- | --- |
| `launchimage.png` | 787 KB | 上游启动图 |
| `find-bg.png` | 366 KB | 上游「发现」页背景 |
| `url-qrcode-both3.jpg` | 90 KB | 二维码 |
| `url-qrcode-both2.jpg` | 89 KB | 二维码 |
| `url-qrcode-both4.jpg` | 53 KB | 二维码 |
| `demo-qrcode-gitee.png` | 42 KB | **上游项目的 Gitee 二维码** |
| `demo-qrcode-github.png` | 42 KB | **上游项目的 GitHub 二维码** |
| `url-qrcode-bak.png` | 30 KB | 二维码备份 |
| `url-qrcode.png` | 30 KB | 二维码 |
| `url-qrcode-bak.jpg` | 21 KB | 二维码备份 |
| `mobile.png` | 16 KB | 上游 README 机型图 |
| `url-qrcode.jpg` | 16 KB | 二维码 |
| `vue-WeChat-chart.png` | 14 KB | **上游项目 star 趋势图** |
| `mobile-dev-guide.png` | 11 KB | 上游 README 素材 |
| `find_icon-shopping.png` | 16 KB | 上游「购物」入口图标 |
| `find_icon-shake.png` | 1.3 KB | 上游「摇一摇」图标 |
| `find_icon-bottle.png` | 0.9 KB | 上游「漂流瓶」图标 |
| `find_icon-locationservice.png` | 0.7 KB | 上游「附近的人」图标 |
| `find_icon-moregame.png` | 0.6 KB | 上游「游戏」图标 |
| `find_icon-qrcode.png` | 0.5 KB | 上游「扫一扫」图标 |
| `recording-signa-l008.png` | 0.3 KB | 上游录音信号图标 |

> 重点：**「摇一摇 / 漂流瓶 / 附近的人 / 购物 / 游戏」是微信 6.x 时代的功能，你的软件里根本没有。**
> 这几个图标 + 5 张二维码 + 上游 star 图，就是最典型的老画面残留。

### 8.2 🟢 确定没用：空目录（0 字节，8 个）

全部是上游自带的空壳，可直接删：

```
images/OfficialAccount/     images/gif/          images/screenshot/
images/album/baiqian/       images/album/guanyu/ images/header/
images/chat/                images/sticker/
```

### 8.3 🔴 发现真问题：图片丢了，但引用还在（会显示空白）

`images/ref/` 目录现在**只剩 1 个文件**（`D.png`），`images/album/` 完全是空的。
但代码和配置里还有一堆指向它们的路径：

| 失效路径 | 写在哪 | 后果 |
| --- | --- | --- |
| `/images/album/baiqian/baiqian01.jpeg`<br>`/images/album/baiqian/baiqian02.jpeg` | `vue-WeChat/src/vuex/contacts.js`（**7 处**） | 通讯录详情页头像空白 |
| `/images/ref/luxianger.png` | `vue-WeChat/src/vuex/store.js`（2 处） | 初始会话数据里陆香儿头像空白 |
| `/images/ref/luxianger.png` | `scene.json`（朋友圈头像） | 朋友圈头像空白 |
| `/images/ref/lux_video.png` | `scene.json`（朋友圈配图） | 「就这么丝滑的下班」配图空白 |
| `/images/album/baiqian/baiqian01.jpeg` | `scene.json`（「新朋友」头像） | 该会话头像空白 |
| 11 个 `/images/ref/*.png`<br>（qun / gongzhong / chenmo / yan / grey_cover / weixin_team / pay / stitch / fuwu…） | `reference_workflow.json` | 套用参考剧本时相关头像全空 |

**这两个文件（`store.js` / `contacts.js`）是被 `main.js` 实际 import 的活代码**，不是死代码。
所以要么补回图片，要么把这些引用一起清掉——目前是「悬空引用」状态。

### 8.4 🟡 存疑

| 文件 | 大小 | 情况 |
| --- | --- | --- |
| `welcome.jpg` | 56 KB | 被 `welcome.vue` 引用，但 `main.py` 里写了 `.welcome{display:none!important}` **永久隐藏** → 永远不会显示，等于死图 |
| `qrcode.jpg` | 34 KB | 被 `my-qrcode.vue` 当「我的二维码」背景用，但**那是上游开源项目的二维码**，扫出来是别人的项目地址 → 建议换成你自己的 |
| `qrcode_vue-wechat.png` | 6.6 KB | 只被上游 README 引用 |
| 老版微信图标组<br>（`contact_*`、`me_more-*`、`find-album-reflash-icon`、`record-cancel`、`recording-bkg` 等 20+ 个） | ~0.3 MB | 代码**仍在引用**，但视觉已被 `enhance/` 的现代皮肤覆盖。<br>删了会破图，留着无害 —— 除非你确认现代皮肤完全不依赖它们 |

### 8.5 🟡 存疑：avatar 库里的「误传大文件」和重复文件

`images/avatar/` 共 33 MB / 100 个文件，其中 **43 个未被任何配置引用**（21 MB）。
它们在编辑器的图片库里都会显示出来，所以不是「废文件」，但里面混了明显传错的东西：

**明显不是头像的**：

| 文件 | 大小 |
| --- | --- |
| `ComfyUI_00001_cqpki_1788164517_20260831_214430_849.png` | **9.2 MB** |
| `学习急救班素材_20260908_155548_722.png` | 1.2 MB |
| `IMG_0512_20260901_140118_982.png` | 1.1 MB |
| `女友生气怎么哄教学_20260905_155312_388.png` | 1.1 MB |
| `聊天技巧图片_20260905_154410_890.png` | 1.0 MB |
| `洗姨妈内裤_20260905_174959_756.jpg` | 0.3 MB |

**字节完全相同的重复文件（5 组，可省 0.93 MB）**：

```
枯木_20260903_141720_316.jpg      ==  枯木_20260908_153043_003.jpg          (675 KB)
女生约出来喝酒_20260903_192052_008.jpg == 女生约出来喝酒_20260905_175037_862.jpg
女生喝酒聊天记录2_20260903_193144_380.jpg == 女生喝酒聊天记录2_20260905_175051_462.jpg
女生喝酒聊天记录3_20260903_193149_810.jpg == 女生喝酒聊天记录3_20260905_175100_372.jpg
哭泣猫咪_20260905_175009_399.jpg  ==  哭泣猫咪_20260908_163147_885.jpg
```

### 8.6 🔴 不能动

`vue-WeChat/src/assets/` 下的 5 个 CSS + 13 个 LESS 文件**全部在被 import**，
是上游微信外观的地基（`enhance/*.css` 是叠在它们上面的覆盖层，不是替代）。
`src/components/` 下 27 个 `.vue` 组件也**全部经路由可达**。这两块没有死文件。

### 8.7 ⏸ 剩下 39 处悬空引用（本轮未动，等你决定）

`images/header/` 和 `images/OfficialAccount/` 两个目录整个丢了，涉及面比 8.3 更广。
我按「是否在你批准的文件里」划分，**只修了你批准的那 4 个文件**，剩下的原样保留：

| 悬空路径 | 处数 | 在哪 | 影响 |
| --- | --- | --- | --- |
| `/images/header/header01.png` | 18 | `main.py` 等 10 文件 | **我方默认头像兜底**：没配头像时显示空白 |
| `/images/header/yehua.jpg` | 18 | `main.py` 等 6 文件 | **对方默认头像兜底**：同上 |
| `/images/header/sunquan.jpg` 等三国人物<br>（孙权/诸葛亮/甄姬/关羽/黄月英/刘备/孙尚香/白浅） | 15 | `contacts.js` 已修，剩 `mobile-contacts.vue`<br>`album.vue` `new-friends.vue` `moments.vue` | 上游 demo 通讯录页，你基本不用 |
| `/images/bg/cover.jpg` `alarm.png` `mao.jpg` `bg01.jpg` | 7 | `dialogue.less` `threeD.less` `moments.vue` | 上游旧背景图 |
| `/images/OfficialAccount/*.JPG` | 5 | `vuex/official-account.js` | 上游「公众号」demo 数据（google-dev / overwatch / lol…） |
| `/videos/demo.mp4` `/videos/xxx.mp4` | 5 | `editor/manual.js` `editor/scene.html` 等 | 手册里的示例路径（本来就该写「示例」） |
| `/images/chat/sticker1.png` | 1 | `enhance/chat_extra.js` | 贴纸图，可能真会显示 |
| `/images/link/副业.jpg` | 1 | `main.py` | 链接卡片默认配图 |

> 判断：`main.py` 那 36 处是**兜底默认值**，只要你每次都在场景里配了头像就不会触发，
> 属于「潜伏 bug」而非当前故障。要不要修，你说了算。

---

## 附：扫描方法

- 对 550 个源码文件（`.py` / `.js` / `.html` / `.vue` / `.css` / `.json`）建文件名引用索引
- 排除归档、`node_modules`、`.git`，避免循环引用误判
- 单独从**真实运行入口**做二次校验：`main.py` / `editor_server.py` / `preview_ui.py` /
  `enhance/_embed_boot.js` / `editor/*.html` / `vue-WeChat/src/`
- 动作库做了三方一致性对比（服务端定义 vs 主程序派发 vs 使用手册）
- 用文件时间戳做了**并发会话活性检测**，把「正在被写」的目录单独隔离出来
- 模型可用性通过官方接口**实测**，非推测

# 创作模式（AI 生成剧本）质量体检报告

生成时间：脚本实时生成 · 只读诊断，未修改任何数据

## 1) 动作表口径差异（三份表互有缺失 = 新功能永远不会生效）

| 表 | 条数 | 用途 |
|---|---|---|
| `editor_server.ACTIONS` | 60 | 拼生成提示词的动作表 |
| `script_translator.ACTION_SCHEMA` | 64 | 校验器 `_valid_action_names()` 的合法动作名来源 |
| `script_translator.ACTION_PARAMS` | 64 | `validate_steps()` 参数归一/默认值 |

**只在前端动作表里、校验器不认的（后果：以它写的规则会被静默降级成 style，永远不生效）**

```
（无）
```

**参数归一缺失（后果：参数不吃默认值、不做类型转换，运行时报错或静默用错值）**

```
（无）
```

## 2) 新功能在「创作链路」里的覆盖（能力卡 + 提示词 + 书写规范 + 校验器）

判据（本轮已改为看「有没有能力卡」，而不是数关键词）：
能力卡 = 「什么时候用 + 片段长什么样」，会随提示词的【能力卡】段落一起发给模型。
没有能力卡的动作，模型只在一行动作表里见过名字 → 实际不会主动用。

| 动作 | 能力卡 | 能力卡内说明 | 可给 AI 写 | 状态 |
|---|---|---|---|---|
| 切换底部面板 | 有 | 聊天中段直接切一下表情/更多面板再收起：真实感最强的新动作 | 是 | ✅ 已接入 |
| 面板切换序列 | 有 | 一口气按既定节奏连切三面板（复刻参考视频的整段节奏） | 是 | ✅ 已接入 |
| 发送emoji | 有 | 我方发微信 3D 黄脸小表情：先弹笑脸面板→逐个点选→一次发送 | 是 | ✅ 已接入 |
| 对方emoji | 有 | 对方发 3D 黄脸小表情（直接上屏，不弹面板） | 是 | ✅ 已接入 |
| 发送表情 | 有 | 我方发贴纸表情包（78×78 小气泡） | 是 | ✅ 已接入 |
| 对方表情 | 有 | 对方发贴纸表情包 | 是 | ✅ 已接入 |
| 对方后台发表情 | 有 | 未打开的会话里，对方发来一张表情 | 是 | ✅ 已接入 |
| 点赞 | 有 | 给朋友圈动态点赞（该动态必须先存在） | 是 | ✅ 已接入 |
| 评论 | 有 | 给朋友圈动态评论：弹菜单→点评论→键盘打字→发送 | 是 | ✅ 已接入 |
| 发朋友圈 | 有 | 我方发一条朋友圈动态（之后可点赞/评论它） | 是 | ✅ 已接入 |
| 编辑朋友圈 | 有 | 预设「我的朋友圈」动态列表（点赞/评论前必须先有动态） | 是 | ✅ 已接入 |
| 打开转账面板 | — | — | 否 | · 内部动作（由别的动作驱动，无需 AI 直接写） |
| 转账 | 有 | 我方转账（默认关闭；仅当规则明确要求时才用） | 是 | ✅ 已接入 |
| 对方转账 | 有 | 对方发来转账（默认关闭；仅当规则明确要求时才用） | 是 | ✅ 已接入 |
| 接收转账 | — | — | 否 | · 内部动作（由别的动作驱动，无需 AI 直接写） |
| 手机状态栏 | 有 | 把手机状态栏切成参考场景（03:14 + 录屏红点），开场用一次 | 是 | ✅ 已接入 |
| 闪回聊天 | 有 | 从主页/朋友圈一帧硬切回聊天（剪辑感，像视频切镜） | 是 | ✅ 已接入 |
| @成员 | — | — | 否 | · 内部动作（由别的动作驱动，无需 AI 直接写） |
| 转发消息 | 有 | 转发一条消息（内容带「转发：」前缀） | 是 | ✅ 已接入 |

未接入的动作：**无**

## 3) 规则库体检（启用 36 条：硬规则 19 / 风格偏好 17；已停用 11 条）

> 已停用 11 条（多为自动治理时的重复/冲突项，可在规则面板里恢复）：`opening_image`、`max_time_marks`、`min_history_messages`、`required_action`、`required_action`、`max_annotations`、`?`、`?`、`?`、`required_action`、`max_message_chars`

### 3.1 同一规则类型出现多条（值不同 = 冲突，值相同 = 冗余）

| 规则类型 | 条数 | 取值 | 判定 |
|---|---|---|---|
| — | — | — | 无重复 ✅ |

### 3.2 互斥规则对（按数值判定：两条规则的数字是否真的无法同时满足）

- 篇幅预算：上限 200 步 / 对白 ≥110 条 / 打字不发 ≥15 处。
  按「每处打字不发配一条删除文字 + 约 12 步油漆」估算最少需要约 **140 步**，余量 **30%**。
  ✅ 可同时满足（两条数字已按预算收敛，提示词与校验器取同一套值）。

- ✅ 已消解：`min_realtime_lines` ⟂ `max_script_steps` —— 已合并成一个「篇幅预算」：max_script_steps=200 时对白按参考比例收敛到 110 条，提示词与校验器取同一套数字。
- ✅ 已消解：`max_people` ⟂ `min_sessions` —— 已把出场人物口径改为「只数 [打开聊天] 真正展开的目标」，历史列表里的联系人不再计入。

**当前启用中的规则里没有未消解的互斥对 ✅**

### 3.3 规则语义错位（值被塞进了不属于它的类型 → 报错文案前言不搭后语）

（未命中）

### 3.4 规则库数值 vs 书写规范块（两处口径是否一致）

- `[打字不发]` **15 处**、插话 **5 处**（配比 1:3.0）。书写规范块与校验器都取自同一份 `rule_numbers()`，不再各写一套数字。
  ✅ 与参考剧本实测比例（约 1:3）一致。
- 篇幅三者（步数上限 200 / 对白下限 110 / 打字不发下限 15）由同一套预算收敛，提示词与校验器不再互相抢额度。

## 4) 用【当前】校验器重跑 41 条生成历史


- 生成当时记录的「一次通过」：**8 / 41**
- 用当前校验器重跑后仍通过：**0 / 41**（0 = 校验器现在几乎拦死所有结果）
- 轮数分布（当时用了几轮）：{1: 5, 2: 10, 3: 26}

### 高频未通过问题 TOP20

（数字已归一成 N —— 同一类问题在不同剧本里的具体条数不同，按类归并才看得出主次；
  括号里是它命中最多的一条原文示例。）

| 次数 | 问题（同类归并） | 示例 |
|---|---|---|
| 20 | 实时对白只有 N 条，少于要求的 N（规则：实时对白不少于 N 条） | 实时对白只有 2 条，少于要求的 110（规则：实时对白不少于 110 条） |
| 14 | 实时对白交替率 N.N 偏高（上限 N.N）：几乎每条都是「我一条、对方一条」，像朗读脚本；要有一段一方连发 N~N | 实时对白交替率 0.86 偏高（上限 0.75）：几乎每条都是「我一条、对方一条」，像朗读脚本；要有一段一方连发 2~3 条。（规则：实时对… |
| 12 | [打字不发] 只有 N 处，少于要求的 N（写法：[打字不发] 技巧说明 \| 停留；正文写「为什么这样聊有效」，不是 | [打字不发] 只有 0 处，少于要求的 15（写法：[打字不发] 技巧说明 \| 停留；正文写「为什么这样聊有效」，不是聊天话术）（规则：至… |
| 11 | 带「插话」的动作只有 N 处，少于要求的 N（本剧本有 N 处 [打字不发]，按约 N:N 配插话即可；格式：[打字 | 带「插话」的动作只有 0 处，少于要求的 5（本剧本有 0 处 [打字不发]，按约 1:3 配插话即可；格式：[打字不发] 技巧说明 \| … |
| 10 | 历史会话里完全没有「我：」的回复（至少要有一个会话是「对方说 → 我回 → 对方再说」）（规则：历史会话整体要有双方 | 历史会话里完全没有「我：」的回复（至少要有一个会话是「对方说 → 我回 → 对方再说」）（规则：历史会话整体要有双方对话（不要求每个会话）） |
| 8 | 【对方表情】引用的素材名「猫咪捂脸」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「哭泣猫 | 【对方表情】引用的素材名「猫咪捂脸」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「哭泣猫咪」。请从【可用素材】清… |
| 8 | 历史会话的时间戳几乎是机械的每分钟一条（N 个相邻间隔里 N 个正好差 N 分钟）：同一会话内部要混着来——同一条分 | 历史会话的时间戳几乎是机械的每分钟一条（6 个相邻间隔里 5 个正好差 1 分钟）：同一会话内部要混着来——同一条分钟内、差 2~3 分钟、… |
| 7 | 整份剧本 N 步，超过上限 N 步：把篇幅压到 N 步左右——砍掉重复的来回和多余的 [对方正在输入]/[等待] 空 | 整份剧本 232 步，超过上限 200 步：把篇幅压到 200 步左右——砍掉重复的来回和多余的 [对方正在输入]/[等待] 空转，保留必要… |
| 4 | 会话数只有 N，少于要求的 N（规则：会话条数必须满N条以上，不可少于N条） | 会话数只有 1，少于要求的 9（规则：会话条数必须满9条以上，不可少于9条） |
| 4 | 出场人物 N 人，超过上限 N（规则：多人聊天控制在N人以内：两个女生 + 一个男主） | 出场人物 4 人，超过上限 3（规则：多人聊天控制在3人以内：两个女生 + 一个男主） |
| 4 | 历史会话里有 N 个会话写了「我：」的回复，超过上限 N（要像微信列表预览：只留 N 个会话带我方回复，其余只留对方 | 历史会话里有 10 个会话写了「我：」的回复，超过上限 2（要像微信列表预览：只留 1 个会话带我方回复，其余只留对方最后 1~2 条，不要… |
| 3 | 同一个表情「猫咪捂脸」用了 N 次，超过上限 N：表情要换着来，不要整段反复发同一张（规则：同一个表情最多重复 N  | 同一个表情「猫咪捂脸」用了 3 次，超过上限 2：表情要换着来，不要整段反复发同一张（规则：同一个表情最多重复 2 次） |
| 2 | 对话消息过少（仅 N 条），请按参考剧本补充完整聊天过程，不要省略。 | 对话消息过少（仅 2 条），请按参考剧本补充完整聊天过程，不要省略。 |
| 2 | 缺少必需动作 [打字不发]（规则：必须使用 [打字不发] 节奏标记，制造博弈张力） | 缺少必需动作 [打字不发]（规则：必须使用 [打字不发] 节奏标记，制造博弈张力） |
| 2 | 【对方表情】引用的素材名「小吃一惊」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「吃惊」 | 【对方表情】引用的素材名「小吃一惊」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「吃惊」。请从【可用素材】清单里… |
| 2 | 【发送表情】引用的素材名「猫咪捂脸」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「哭泣猫 | 【发送表情】引用的素材名「猫咪捂脸」在图片库里不存在（运行时会破图或回落成同一张默认表情）；清单里最接近的是「哭泣猫咪」。请从【可用素材】清… |
| 2 | 历史会话里「苏苏」连续发了 N 条，超过上限 N（历史要有来有回，不要一个人刷屏）（规则：历史会话里同一人最多连续  | 历史会话里「苏苏」连续发了 3 条，超过上限 2（历史要有来有回，不要一个人刷屏）（规则：历史会话里同一人最多连续 2 条） |
| 2 | 【发送表情】引用的素材名「柴犬敲木鱼」在图片库里不存在（运行时会破图或回落成同一张默认表情）。请从【可用素材】清单里 | 【发送表情】引用的素材名「柴犬敲木鱼」在图片库里不存在（运行时会破图或回落成同一张默认表情）。请从【可用素材】清单里逐字挑一个，不要自己描述… |
| 1 | 开头没有图片（规则：剧本开头插入图片，增强画面感与代入感） | 开头没有图片（规则：剧本开头插入图片，增强画面感与代入感） |
| 1 | 【我方打字】内容过长（N 字，上限 N）：不过那句"穿不了" 说明你平时挺不放…（规则：[我方打字] 的话术要短小精 | 【我方打字】内容过长（21 字，上限 20）：不过那句"穿不了" 说明你平时挺不放…（规则：[我方打字] 的话术要短小精悍） |

## 5) 格式容错体检（被静默丢掉的行 = 剧本凭空变短 → 触发「对白不够」→ 重写死循环）


判据：`main.parse_script_text` 遇到无法识别的行会 `print('[剧本警告] …已跳过')` 后丢弃该行。
这里捕获这些输出，统计「模型写了但被扔掉」的指令。

- 有 2 / 41 条生成结果里存在被静默丢弃的指令行。

| 次数 | 被丢掉的行 |
|---|---|
| 1 | `[剧本警告] 第 N 行未知指令 [为我方打字]，已跳过` |
| 1 | `[剧本警告] 第 N 行未知指令 [我等打字]，已跳过` |
| 1 | `[剧本警告] 第 N 行格式无法识别，已跳过：[返回主页` |

对策：把「让模型输出 JSON、本地渲染成 [指令]」当成生成主路径，
格式类失败会一次性消失；文本解析只留作兼容用户手写的老路径。

## 6) 参考库体检（每条参考自己能不能过当前校验器）

整篇（当风格模板）**3** 条 / 片段（当能力示范）**18** 条。提示词里最多取 2 条整篇 + 6 条片段。

| 参考 | 行数 | 解析步数 | 问题数 | 最严重的问题 |
|---|---|---|---|---|
| 蕾蕾 · 10 个会话 · 我也只是做了普通朋友该做的 | 223 | 189 | 4 | 历史会话里有 9 个会话写了「我：」的回复，超过上限 2（要像微信列表预览：只留 1 个会话带我方回复，其余只留对方最后… |
| 没烦恼、 · 9 个会话 · 哈哈哈哈没有啊 | 168 | 126 | 7 | [打字不发] 有 2/9 处写成了聊天话术草稿而不是给观众看的技巧说明（例：除了你呀，你是唯一一个；想要聊天秘籍的兄弟 … |
| susu · 9 个会话 · 怎么突然听到你跟我说这个 | 219 | 194 | 2 | 历史会话里「宝宝」连续发了 3 条，超过上限 2（历史要有来有回，不要一个人刷屏）（规则：历史会话里同一人最多连续 2 … |

⚠️ 下面这些整篇参考**自己过不了当前规则**（拿它当 few-shot 等于在教坏习惯）：
- 《蕾蕾 · 10 个会话 · 我也只是做了普通朋友该做的》 —— 4 个问题
- 《没烦恼、 · 9 个会话 · 哈哈哈哈没有啊》 —— 7 个问题

建议：整篇参考只留 1~2 条最干净的（或者等「自动沉淀」攒出新的零问题剧本后再来挑），
其余删掉或降级成片段；新功能一律用【片段示例】教，不要靠整篇。

（提示词只会取 2 条整篇当风格模板，所以「库里有多少」不等于「模型看到多少」；
 `search_references` 会优先挑评分高、被采用次数多的。）

## 7) 真正的头号堵点（把 41 条历史的问题归类，共 198 个）

规则冲突只占一部分；下面按出现次数排序 —— 先修排在最前面的，收益最大。

| 次数 | 占比 | 堵点 | 已经做的处置 |
|---|---|---|---|
| 35 | 18% | ② [打字不发] 写成了聊天话术 / 数量不够 | 参考剧本自己在这一点上就不一致（它也没写满），建议整篇参考只留最干净的一条，其余用【片段示例】。 |
| 34 | 17% | ① 素材名编造（写了图库里没有的图/表情） | 提示词的【可用素材】块已改成「编号 + 反面例子 + 逐字挑」；报错也会直接给出「清单里最接近的是 X」。 |
| 30 | 15% | ⑫ 其他 / 单条偶发 | — |
| 20 | 10% | ⑤ 实时对白条数不够 | 已按参考剧本比例收敛（110 条），提示词与校验器取同一套数字。 |
| 18 | 9% | ⑥ 历史会话写法（写成了一问一答 / 同一人刷屏） | 规范块 B 段已给出「微信列表预览」的标准样本。 |
| 14 | 7% | ④ 实时对白没有连发（我一条你一条像朗读） | 规范块要求「至少一段一方连发 2~3 条」。 |
| 11 | 6% | ⑧ 单条话术过长 | 上限 20 字；[打字不发] 的技巧说明不再计入。 |
| 8 | 4% | ⑨ 人物 / 会话数量 | 已消解互斥：出场人物只数 [打开聊天] 的目标。 |
| 8 | 4% | ③ 历史会话时间戳机械 +1 分钟 | 已在规范块里给出「同分钟内/差2~3分钟/隔十几分钟」的混排写法。 |
| 7 | 4% | ⑦ 步数超上限 | 给了篇幅预算，要求「靠拆句拉节奏，不靠堆空转指令」。 |
| 7 | 4% | ⑩ 同一表情重复太多 | 规则上限 2 次。 |
| 6 | 3% | ⑪ 插话写法 / 数量 | 统一为「打字不发 ÷ 3」，规范块与规则库同一来源。 |

> 注：这批是**旧规则下生成的旧成品**，所以它反映的是「历史痛点分布」，
> 而不是「现在的生成质量」。现在的改动要等下一次真实生成才能验证。

---
重跑本报告：`py _diag_generate.py`
